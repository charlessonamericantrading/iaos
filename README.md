# AgentOS

[![kernel-ci](https://github.com/charlessonamericantrading/iaos/actions/workflows/kernel-ci.yml/badge.svg)](https://github.com/charlessonamericantrading/iaos/actions/workflows/kernel-ci.yml)

An "AI-native" operating system project, explored in two independent, **not yet integrated** directions:

1. **[`agentos-kernel/`](agentos-kernel/)** — a real bare-metal kernel written in Rust (`#![no_std]`), booting on actual x86_64 hardware/QEMU. This is the actively developed half of the project.
2. **[`src/`](src/) + [`public/`](public/)** — a TypeScript/Express dashboard that simulates what an AI-agent OS's control plane might look like (scheduler, memory, sandbox). It is a UI mock: no real model calls, no persistence, everything in-memory. Parked in favor of the kernel.

There is no bridge between the two yet — `src/bridge.ts` only POSTs one canned sample event to demonstrate the shape of an API, it does not talk to the kernel.

## Current status (honest version)

The kernel **boots for real** in QEMU via the [`bootloader`](https://github.com/rust-osdev/bootloader) crate and reaches an interactive prompt. What's actually real hardware/software vs. simulated scaffolding, as of this commit:

| Subsystem | Status |
|---|---|
| Boot (`bootloader` crate, BIOS) | **Real.** Verified boot to `AgentOS>` prompt. |
| GDT / IDT / CPU exceptions | **Real.** breakpoint, double-fault (own IST stack), page-fault, GPF, divide-error, invalid-opcode all wired and verified via a live `int3` self-test. **`gdt.rs` (Fase 68) now also carries the foundational pieces a real ring-3/usermode transition needs, deliberately built as its own first step of a genuinely new, dedicated arc** (`SYS_TENSOR_EVAL` and this kernel's other syscalls are real, per the row below, but have been structurally unreachable from anywhere but this kernel's own ring-0 self-tests until this arc closes): real `Descriptor::user_data_segment()`/`user_code_segment()` entries appended to the GDT (confirmed genuinely DPL=3 by reading each selector's own Requested Privilege Level bits back, not just assumed from which constructor was called), and the TSS's `privilege_stack_table[0]` (RSP0) - the kernel stack the CPU is required to switch to automatically on any control transfer that RAISES privilege, e.g. an interrupt/exception/`syscall` arriving while in ring 3 - now points at a real, dedicated stack instead of the TSS's own zeroed default (which would triple-fault the instant anything ever tried to use it). Verified identical across 3 repeated boots, zero regression anywhere in the rest of the self-test suite - a meaningful check given how early and foundational a GDT change is, this being the first change to that specific structure since the kernel's very first exception-handling work. Does **not** yet mean ring-3 code can actually run: no IDT gate has DPL=3 (so nothing can legally reach the kernel from ring 3 via `int`), no page in this kernel's address space is marked user-accessible (ring-3 code couldn't read or execute anything yet even if it started running), and nothing anywhere performs the actual privilege-lowering control transfer (`iretq` to ring 3, or `sysret`) - each of these is real, substantial follow-on work, matching this project's own established multi-Fase pattern for large features. **`interrupts.rs` (Fase 69) closes the FIRST of those three gaps: a real interrupt gate at vector `0x80` (the classic Unix/Linux software-interrupt syscall convention) is now registered with `set_privilege_level(Ring3)`, unlike every other entry in this kernel's IDT (which all default to DPL=0, meaning ring-3 code attempting `int` on any of them would itself take a #GP - by design, since none of those are meant to be a controlled kernel entry point).** Verified by actually EXECUTING `int 0x80` right now, not just reading the gate's own DPL bits back in isolation: from ring-0 (no ring-3 code exists yet to test the actual privilege-lowering path, but `CPL=0 <= DPL=3` is always a permitted invocation) the handler genuinely fires, incrementing a counter a self-test observes (`count_delta=1`) - real, functional proof the gate is correctly wired, mirroring the same "actually trigger it" discipline the existing `int3` breakpoint self-test already established. Deliberately minimal beyond that: the handler doesn't yet read the caller's general-purpose registers or dispatch into the real `syscall::dispatch_syscall` - `extern "x86-interrupt"` handlers don't receive those as ordinary parameters, so doing this correctly needs inline assembly or a hand-written naked wrapper (this kernel's `context_switch.rs` already has precedent for exactly that kind of code, still flagged for human review) - real, separate follow-on work, not crammed into this same Fase. Verified identical across 3 repeated boots, zero regression anywhere in the rest of the self-test suite. **`memory::user_page` (Fase 70) closes the SECOND of the three remaining ring-3 gaps: a real, dedicated page mapped at `0x5555_5555_0000` with `PRESENT | WRITABLE | USER_ACCESSIBLE`, deliberately separate from the heap (which stays kernel-only).** A genuinely easy-to-miss correctness subtlety was checked, not assumed, before writing any code: x86_64 ANDs the effective access permission across EVERY page-table level (PML4/PDPT/PD/PT), not just the final entry, so a single non-user-accessible intermediate level anywhere in the walk would silently block ring-3 access regardless of what the leaf says - confirming the new page's virtual address shares NO page-table level with the heap's own range (PML4 index 170 vs. 136) was what made it safe to rely on the `x86_64` crate's own automatic behavior (`Mapper::map_to` derives new intermediate tables' flags directly from the leaf flags passed in) rather than needing the lower-level `map_to_with_table_flags` API explicitly. Verified two independent ways: `user_accessible=true` is the real leaf PTE flag read back through the page tables (via `Translate`, not just trusting a successful `map_to` call), and `write_read_back_ok=true` is a real byte-pattern write-then-read through the mapping (ring-0 can access a `USER_ACCESSIBLE` page just as freely as any other - that bit only ever restricts ring-3) - structural and functional proof together, not just one or the other. Still entirely a ring-0 operation: nothing runs in ring-3 yet to prove this bit makes an actual difference - that remains the single biggest remaining step in this arc. Verified identical across 3 repeated boots, zero regression anywhere in the rest of the self-test suite. **`ring3.rs` (Fase 71) is that remaining step: the first genuine ring-0 -> ring-3 privilege-lowering transition this kernel has ever performed, completing the arc's core goal.** A real `iretq` stack frame is built by hand (`SS`/`RSP`/`RFLAGS`/`CS`/`RIP` pushed in the exact order `iretq` expects, `CS`/`SS` using Fase 68's own `user_code_selector`/`user_data_selector`, `RIP`/`RSP` pointing into Fase 70's own user-accessible page) and executed - and the "ring-3 program" waiting there is deliberately just one byte: `cli` (`0xFA`). `cli`/`sti` require `CPL <= IOPL` (IOPL defaults to 0), so executing it at `CPL=3` - which reaching and fetching it at all already requires - triggers a real `#GP`, caught by the EXISTING, unmodified GPF handler from this kernel's own original exception-handling work. Worked correctly on the very first attempt: the fault's own `InterruptStackFrame` dump shows `instruction_pointer` at exactly the user page's own address and, most conclusively, `code_segment: SegmentSelector { index: 5, rpl: Ring3 }` / `stack_segment: SegmentSelector { index: 4, rpl: Ring3 }` - the CPU's own reported privilege level at fault time, directly confirming genuine ring-3 execution, not inferred or assumed. Identical across 3 repeated local verification boots. **Deliberately exposed only as a new `ring3test` shell command, never invoked automatically at boot**: since this test intentionally ends in a permanent halt, baking it into the unconditional boot sequence would either remove the interactive shell from every real boot (if run before the prompt) or prevent this kernel's own later self-tests from ever completing (if run after) - a real, structural tension between "prove a deliberately destructive command works" and "keep the normal boot healthy," not an oversight. Consequently NOT part of the shared, automated CI boot-test assertions either (documented as such directly in `kernel-ci.yml`) - verified instead via a real, repeatable local test (temporarily invoked, removed before committing) exactly as described above. **Fase 72 completes the arc's remaining gap from Fase 69's own notes: the DPL=3 gate now reads REAL caller registers and dispatches to the REAL `syscall::dispatch_syscall`, instead of just incrementing a counter.** `extern "x86-interrupt"` handlers - what every other IDT entry in this kernel uses - are compiler-generated wrappers that only ever expose the pushed `InterruptStackFrame`, never the general-purpose registers (`RAX`/`RDI`/`RSI`/`RDX`) a real syscall convention uses to carry the call number and arguments. Vector `0x80` is now instead registered via `Entry::set_handler_addr` pointing directly at a hand-written `#[unsafe(naked)]` entry stub (`syscall_entry_asm`) - no compiler-generated prologue/epilogue at all, the same category of risk as `context_switch.rs`'s own asm, worked through with the same care: all 15 general-purpose registers are saved (deliberately more conservative than the real Linux/SysV convention, which only guarantees callee-saved registers survive), the real `RAX`/`RDI`/`RSI`/`RDX` are loaded into the SysV calling convention's own argument registers to call a normal Rust dispatch function, its `u64` return value is written back into the saved-`RAX` slot, every register is restored, and a real `iretq` resumes the caller. The exact push/pop offsets and the resulting stack alignment across the `call` boundary (interrupt frame + 15 pushes = 160 bytes, an exact multiple of 16, satisfying the SysV ABI's alignment requirement) were computed and verified by hand before writing any code, not assumed. Proven two ways: the existing ring-0 self-test (Fase 69's own) now pins real arguments (`SYS_SERIAL_PRINT`, `0`, `0`, `0`) instead of leftover register garbage and checks the real returned value (`dispatch_ret=0`); and a new, genuinely ring-3-originated test (`ring3.rs`'s `run_ring3_syscall_test`, the `ring3syscall` shell command) writes a real three-instruction "program" (`mov eax, 1` / `int 0x80` / `cli`) into Fase 70's user page and executes it from ring-3 - the resulting fault's `instruction_pointer` lands exactly at byte offset 7 (right after the 5-byte `mov` and 2-byte `int 0x80`), direct proof execution genuinely RESUMED in ring-3 after a real syscall round trip, not just that ring-3 could be entered once. Byte-for-byte identical across 3 repeated local verification boots (same discipline, and the same "ends in a permanent halt by design" reasoning, as `ring3test` above - also deliberately not CI-automated, for the identical reason). What remained open after Fase 72: a safe way to return from ring-3 to ring-0 *without* ending in a deliberate fault. **Fase 73 closes exactly that gap - the first ring-3 test in this whole arc that does NOT end in a permanent halt.** `enter_ring3` (a second hand-written `#[unsafe(naked)]` function) adapts `context_switch.rs`'s own `switch_to` trick across a ring-3 boundary: it saves the same 6 callee-saved registers (`rbp`/`rbx`/`r12`-`r15`) `switch_to` already established, in the same order, stores the resulting RSP into a static, then builds and executes a real `iretq` frame exactly like Fase 71's own test. The new piece is the return direction: a SECOND, dedicated DPL=3 interrupt vector (`0x81`, deliberately separate from the syscall vector rather than a special syscall number - simpler to reason about than one handler that sometimes resumes ring-3 and sometimes doesn't) whose handler discards the ring-3 context entirely, switches RSP to the stack `enter_ring3` saved, pops the same 6 registers, and returns - landing back in `enter_ring3`'s own ORIGINAL caller with a real value, as if it had returned normally. Two things make this safe rather than merely convenient, both confirmed rather than assumed: every IDT entry in this kernel defaults to a 64-bit INTERRUPT gate (confirmed via the `x86_64` crate's own `EntryOptions::minimal()`), which unconditionally clears `RFLAGS.IF` on entry, so a timer tick can never fire mid-transition and corrupt the shared `RSP0` stack; and `RSP0` (the TSS's dedicated ring3-entry stack), the kernel's own ordinary stack, and the ring-3 program's own stack (Fase 70's user page) are three genuinely distinct stacks that never overlap. Hit two real, instructive errors along the way, not glossed over. First, a build error: the naked stubs' direct `[symbol]` memory operands linked with `relocation R_X86_64_32S cannot be used against local symbol` - this kernel builds as a position-independent executable (`-pie`), which forbids absolute 32-bit addressing; fixed by making the RIP-relative addressing explicit (`[rip + {symbol}]`), the standard fix for exactly this class of PIE/PIC error. Second, a genuine CI-caught regression, not caught locally first: `ring3_exit_entry_asm` exits via a plain `ret`, not `iretq` - and since every IDT entry in this kernel defaults to a 64-bit INTERRUPT gate that unconditionally clears `RFLAGS.IF` on entry, a plain `ret` (unlike `iretq`, which restores the FULL `RFLAGS` as part of its own defined behavior) left interrupts permanently disabled for the rest of the kernel's life after the very first ring-3 exit. Every self-test that doesn't specifically depend on a NEW interrupt firing still ran fine; the preemptive scheduler demo - which genuinely cannot finish without real timer ticks forcing a task switch - hung forever, timing out the whole CI job (surfaced as `CURSOR REGRESSION: expected exactly 2 'help ->' dispatches ... got 1`, a later, unrelated-looking self-test that simply never got the chance to run). Root-caused directly from the CI log rather than guessed at, then fixed by saving `RFLAGS` (`pushfq`) alongside `enter_ring3`'s own 6 callee-saved registers and restoring it (`popfq`) in `ring3_exit_entry_asm` before the matching pops - confirmed by re-running locally with a long enough timeout to reach the preemptive demo, which now shows real, nonzero per-task tick counters and both tasks reaching `DEAD` state. Verified with a real three-syscall ring-3 program (`mov eax,1`/`int 0x80`/`mov eax,42`/`int 0x81`, with a trailing `cli` as a safety-net fault detector that only ever executes if the exit path somehow failed to redirect): `exit_code=42` matches exactly, and - the real proof this isn't just "entering ring-3 worked again" - the very next self-test in the boot sequence (`FRAME ALLOC`) fires immediately afterward, direct evidence the kernel's own execution genuinely continues rather than halting. Because this test doesn't end in a halt, it's the first ring-3 test that runs unconditionally as part of every normal boot and carries a normal CI assertion, no opt-in shell command needed. Byte-for-byte identical across 3 repeated boots (post-fix). What remained open after Fase 73: this only proves a ring-3 program can voluntarily exit ONCE with a scalar value; a pointer-validation boundary for syscall arguments (see the Syscall dispatcher row below for Fase 74's own fix); and, still open, nothing yet lets the kernel run MULTIPLE ring-3 programs or schedule between ring-3 and ring-0 work the way the existing cooperative/preemptive schedulers do for kernel-only tasks. **Fase 75 closes a smaller, related gap: `dispatch_syscall` now knows, for real, whether a given call came from ring-3 or ring-0** - information Fase 74's own pointer-validation work explicitly needed but didn't yet have (requiring `USER_ACCESSIBLE` unconditionally would have rejected the kernel's own legitimate ring-0 self-tests, so that Fase deliberately checked only `PRESENT`). `syscall_entry_asm` (Fase 72's own naked stub) now reads one more thing before calling into Rust: the CPU's own interrupt frame - pushed BEFORE the stub's 15 saved registers, so it sits at a fixed, hand-verified offset (128 bytes) above them - always has `RIP` as its first field and `CS` as its second, regardless of whether the entry was same-privilege (3 fields) or privilege-elevating (5 fields, a real ring-3 call); either way `CS`'s low 2 bits are the CPU's own Requested Privilege Level at the exact moment `int 0x80` fired. Threaded through as a new 5th parameter to `handle_real_syscall`, which now logs it directly. Deliberately does NOT yet use this value to change any syscall's behavior - proving it reads correctly is this Fase's own complete scope; using it to enforce `USER_ACCESSIBLE` for ring-3-originated pointers is real, separate follow-on work. Verified with zero new self-tests needed: the EXISTING ring-0 self-test (Fase 69's own bare `int 0x80`) now shows `caller_rpl=0`, and the EXISTING ring-3 test (Fase 73's own `run_ring3_exit_test`, which itself performs a real syscall via `int 0x80` before exiting via `int 0x81`) shows `caller_rpl=3` - the same mechanism producing genuinely different, correct results depending on where it was actually invoked from, not a hardcoded constant. Byte-for-byte identical across 3 repeated boots. **Fase 79 takes the first, narrower step toward that still-open "schedule between ring-3 and ring-0 work" gap Fase 73 named**: can the timer interrupt even tell it interrupted RING-3 code specifically, not just that a tick happened? `scheduler::preemptive`'s existing timer-driven preemption (see that module's own doc) only ever switches between two ring-0 tasks via `context_switch.rs`'s `switch_to` - a trick that works there specifically because every such task is always resumed from the exact same nested position inside the timer handler's own call frame. A genuine ring-3 task, preempted at an arbitrary point with an arbitrary register state and a different privilege level, cannot be resumed that way at all - it needs the timer handler itself to save and restore a FULL register + privilege context, a substantially bigger, separate piece of work this Fase deliberately does not attempt. `timer_interrupt_handler` stays an ordinary `extern "x86-interrupt"` fn (no naked asm needed for this step, unlike `syscall_entry_asm`'s own CS.RPL read, which needed one for an unrelated reason - exposing general-purpose registers): `InterruptStackFrame`'s `code_segment` field already exposes the interrupted context's privilege level for free, via the same `rpl()` accessor Fase 75 already established, confirmed by reading the vendored `x86_64` crate's own source directly rather than assumed. A new `TIMER_TICKS_WHILE_RING3` counter increments whenever a tick's own `code_segment.rpl()` reads `Ring3`. Verified with a real ring-3 program that spins in a 20,000,000-iteration hand-encoded decrement loop (generous headroom over the ~55ms one tick takes at this kernel's ~18.2Hz PIT rate - chosen the same empirically-verified way `ata.rs`'s own poll-iteration bound was, not just guessed) before exiting voluntarily via the same `enter_ring3`/`int 0x81` mechanism Fase 73 established: `exit_code=77` confirms the loop ran to completion, and `ticked_during_ring3=true` confirms the counter genuinely advanced *during* that specific ring-3 execution window (checked before/after the call), not merely that some unrelated tick occurred elsewhere in the boot. Worked correctly on the very first attempt (2 real ticks observed during the loop on the first run, 1 on subsequent runs - real, if small, timing variance, not a bug), identical `exit_code`/`ticked_during_ring3=true` results across 3 repeated boots, zero regression across the entire prior self-test suite. Genuine multi-ring3-process scheduling itself remains real, separate, substantially larger follow-on work. **Fase 80 closes a real gap Fase 79's own verification exposed while thinking through what it made newly possible to check**: `scheduler::preemptive::tick()` called `switch_to` unconditionally whenever preemption was enabled, with no awareness that the interrupted context could be ring-3 code - and `switch_to`'s own trick (unwinding back up through the interrupted call stack to the timer handler's compiler-generated epilogue) only works between two ring-0 tasks, which are always resumed from the identical nested position; a ring-3 program, interrupted at an arbitrary point with an arbitrary register state and privilege level, cannot be resumed that way at all. This had never actually been exercised - ring-3 execution and ring-0 preemption enablement had always been sequential in every earlier self-test, never concurrent - so it wasn't a prior regression, just a real, previously-latent blocker for eventual ring-3-aware scheduling. Fixed by threading a new `interrupted_ring3: bool` from `timer_interrupt_handler` into `tick()`, which now skips task-switching (and the tick-budget countdown) entirely whenever it's true - treating a tick that interrupts ring-3 as invisible to this scheduler, as if no time passed there at all. Verified with a new test running ring-0 preemption and a real ring-3 program CONCURRENTLY for the first time: a shorter ring-0 demo starts, the tick budget is captured, a ring-3 spin loop runs (several real ticks land during it), the budget is captured again - `budget_untouched_during_ring3=true` confirms it didn't move - then the ring-0 demo is allowed to finish normally, with both tasks' own counters confirmed to have genuinely advanced afterward (`tasks_advanced_after=true`), not just that nothing crashed. **A real reliability gap in Fase 79's own test was also caught and fixed during this verification, not glossed over**: a local rerun of the already-CI-green Fase 79 test showed `ticked_during_ring3=false` once - the original 20,000,000-iteration loop count turned out to be marginal (QEMU's TCG JIT doesn't run this tiny loop at a fixed, predictable speed run-to-run), not safely above one real tick period as intended. Raised to 150,000,000 for both this Fase's own spin loop and Fase 79's, re-verified across several repeated local boots (consistently several real ticks landing, never zero) before trusting either again. Identical `exit_code=88`/`budget_untouched_during_ring3=true`/`tasks_advanced_after=true` across 3 repeated boots, full-diff regression clean (only the usual known-benign RTC/tick-count/local-disk-persistence variance), zero regression across the entire prior self-test suite. **Fase 81 lays the necessary foundation for the "genuine multi-ring3-process scheduling" work Fase 79/80 both deliberately deferred**: every ring-3 mechanism through Fase 80 (`run_ring3_test`, `run_ring3_syscall_test`, `enter_ring3`/`run_ring3_exit_test`) enters ring-3 via a plain Rust function call that builds and executes an `iretq` frame directly from its own arguments - simple, and correct for "call into ring-3 and wait for it to finish," but not the shape a real scheduler needs, since `scheduler::preemptive::tick()` can only ever safely switch between two ring-0 tasks resumed from the identical nested `switch_to` call-frame position, never an arbitrary interrupted ring-3 context. This Fase does NOT yet solve that (genuine mid-flight preemption of a running ring-3 program remains real, separate follow-on work) - it proves the necessary prerequisite: a ring-3 "task" can be entered the SAME way a ring-0 cooperative/preemptive task is, via `switch_to` landing on a freshly prepared stack, instead of `enter_ring3`'s own bespoke calling convention. The mechanism mirrors `context_switch.rs`'s own `prepare_initial_stack` closely: a new `prepare_ring3_initial_stack` lays out a fresh kernel-side stack so `switch_to`'s own `ret` lands on `ring3_entry_trampoline` - a naked function whose ENTIRE body is a bare `iretq` - rather than an ordinary ring-0 `entry` function, correct because the same fake stack also carries a complete 5-field `iretq` frame (RIP/CS/RFLAGS/RSP/SS) sitting exactly where RSP points the instant the trampoline is reached; unlike `prepare_initial_stack`'s six zeroed callee-saved slots (whose exact sub-order never mattered, since they're all zero anyway), these five fields are all genuinely different values and had to be written in the precise reverse order of how `iretq` consumes them, verified by hand against `switch_to`'s own pop sequence before writing any code. The return trip needed its own new mechanism too, not `enter_ring3`'s: a THIRD, dedicated DPL=3 interrupt vector (`0x82`, deliberately separate from both `0x80` and `0x81` for the same "one handler shouldn't branch on which convention applies" reasoning that already justified keeping `0x81` separate from `0x80`) whose handler (`ring3_task_exit_entry_asm`) resumes whoever called `switch_to`, using `switch_to`'s OWN pop convention (`r15`/`r14`/`r13`/`r12`/`rbx`/`rbp`, no saved RFLAGS) rather than `enter_ring3`'s. Needed the same category of interrupt-gate fix Fase 73 discovered the hard way - every IDT entry defaults to a 64-bit INTERRUPT gate, which unconditionally clears `RFLAGS.IF` on entry - but the fix itself differs: since `switch_to`'s own callers never had their RFLAGS captured to begin with (unlike `enter_ring3`, which added its own `pushfq` specifically to give `ring3_exit_entry_asm`'s `popfq` something to restore from), a bare `sti` immediately before the final `ret` re-enables interrupts directly, and x86's documented one-instruction "sti shadow" guarantees the `ret` itself still executes first - no window where a real timer tick could land mid-restore. Verified with a ring-3 "program" that computes 5+4+3+2+1=15 in `eax` via a genuine loop (not just an immediate exit) before exiting through the new vector: `exit_code=15` is real, exact proof of correct execution through this brand-new bootstrap path end to end. Spawns no PCB entries of its own (unlike Fase 80's own test), so - unlike that one - there's no hardcoded-PID ordering constraint on where its call sits in the boot sequence. Worked correctly on the first attempt; identical `exit_code=15` across 2 independently-fresh-disk boot-tests, full-diff regression clean against both (only the usual known-benign RTC/tick-count/spin-loop-counter-magnitude variance), and the complete pre-existing 109-assertion CI suite re-verified in full (not just the new one) before trusting it, per this project's own established post-Fase-80 verification discipline. Genuine mid-flight ring-3 preemption - actually switching a running ring-3 program out for something else and back again later - remains real, separate, substantially larger follow-on work. |
| 8259 PIC + hardware interrupts | **Real.** PIC remapped to vectors 32-47 (avoids colliding with CPU exception vectors 8-15), interrupts enabled, timer (IRQ0) and keyboard (IRQ1) are interrupt-driven, not polled. |
| Physical memory / paging / heap | **Real.** Frame allocator walks the actual `BootInfo` memory map (skips regions already used by the kernel/bootloader); the heap is properly mapped page-by-page before use. `alloc` (`Vec`, `Box`, `String`) works. 1 MiB heap (raised from an initial 100 KiB after `cat`-ing a real file proved that too small - see FAT12 row below). The same frame allocator instance is now also reachable *after* boot's initial heap setup via a global `memory::frame_allocator::allocate_frame()` - a prerequisite for any future code needing dedicated physical memory outside the heap (a real e1000 TX/RX descriptor ring being the motivating case - see NIC row below). Deliberately the *same* allocator, not a fresh one built later from the same memory map: a fresh one would start handing out frame 0 again, which the heap already owns by then. Verified by allocating 3 frames after boot and write/read-back-testing a pattern through each (via the same `PHYS_MEM_OFFSET` mechanism the VGA buffer and e1000 probe use) - came back distinct and exactly 4 KiB apart (`0x163000`, `0x164000`, `0x165000` locally), proving these are genuine, usable, non-overlapping memory, not just plausible numbers. |
| Agent scheduler (`ps`-visible) | **Now really unified with both the cooperative and preemptive schedulers** (next two rows): `spawn_cooperative` and `run_preemptive_demo` both register real PCB entries via `SCHEDULER.lock().spawn(...)`, and each scheduler keeps its entries' `state`/`stack_pointer` honest as tasks actually run, yield/get switched, and finish - `ps` genuinely reflects live scheduler state now, not just a fixed table. Still partial for one thing: the 4 processes spawned directly at boot as a self-test (`kernel-supervisord` etc.) have PCB entries but no real stack/registers behind them - nothing ever actually schedules them as tasks. |
| Cooperative task scheduler | Real, and now **PCB-unified** (previous row). `scheduler/context_switch.rs`'s `#[unsafe(naked)]` `switch_to` swaps RSP + the 6 callee-saved registers between stacks (xv6-style) - `spawn_cooperative`/`yield_now`/`finish_current_task` build a priority-ordered N-task cooperative scheduler (up to 4 tasks) on top of it, and each task is now a real `ps`-visible process, not just an internal one. Verified two ways in QEMU: `ps` dispatched from *inside* a running task shows it `RUNNING` with a real (nonzero, heap-range) stack pointer while its not-yet-scheduled siblings still show `READY` with no stack pointer recorded yet; `ps` again after the demo shows all three `DEAD` (terminated) with distinct real stack pointers exactly `TASK_STACK_SIZE` (16 KiB) apart, matching their actual heap layout - strong evidence these are genuine addresses, not placeholders. A task only ever changes on its own voluntary `yield_now` call. |
| **Preemptive scheduler (real)** | Real, and now **PCB-unified too**. `scheduler/preemptive.rs` hooks the same `switch_to` primitive into the timer IRQ (`interrupts.rs`) so tasks that *never* call `yield_now` at all still get forced off the CPU - proven with two infinite, non-cooperating loops that both end up with large, nonzero counters after a timed window, which is only possible if the timer genuinely reclaimed the CPU from each without asking. Two real, subtle bugs surfaced and got fixed getting here, both the same root cause: state shared between an interrupt handler and normal code needs `AtomicBool`/`AtomicU64`, not a plain `static mut` - in a release+LTO build, the compiler hoisted a `while flag { hlt() }` poll loop's read out entirely (an infinite hang, since it never noticed the interrupt handler had cleared the flag), and separately treated a counter only ever incremented inside an infinite loop with no in-function reader as having no provable side effect. Neither was caught by clippy or by reasoning about the design alone - only by actually booting it and getting a genuinely wrong/hung result, then diagnosing why. **PCB unification needed real interrupt-safety care**, unlike the cooperative case: `tick()` runs inside the timer interrupt handler, so it uses `SCHEDULER.try_lock()`, never the blocking `.lock()` - taking a normal lock there would deadlock permanently on this single core if the timer ever fired while normal code held it mid-update, since that code could never resume to release it until this very handler returns. A skipped update (vanishingly rare, and harmless - `ps` is diagnostic-only) self-corrects the next tick, ~55ms later. Verified the same two ways as the cooperative case, plus 3 repeated full boots to check for interrupt-timing-dependent flakiness specifically (none observed - identical serial output every run). **Fase 87 closes a real, self-admitted gap this exact row used to carry**: `PreemptTask` always tracked a `priority` field (visible in `ps`, e.g. `[HIGH]`/`[NORM]`/`[BG]`), but `tick()` itself ignored it completely, always alternating strictly `0<->1` regardless - an honest limitation this project's own code comments called out directly, not a hidden bug. `tick()` now finds the highest actual priority among the populated task slots, then round-robins ONLY within that top tier (starting right after whichever task last ran, wrapping) - deliberately NOT `context_switch.rs`'s own cooperative `yield_now()` rule (lowest slot index always wins ties), since applying that rule here would mean two EQUAL-priority tasks never alternate at all, silently breaking this row's own "both counters > 0" guarantee above. Round-robin-within-the-top-tier reduces to the OLD strict alternation byte-for-byte whenever both tasks share one priority (the existing demo above is completely unaffected, re-verified unchanged), while a genuinely higher-priority task now wins every single comparison and monopolizes the CPU completely - proven by a new test spawning task-preempt-0 as `Priority::High` and task-preempt-1 as `Priority::Background` (genuinely unequal, unlike every prior caller of the shared `start_demo` helper): `task1_delta=0` exactly, not just smaller - the lower-priority task gets ZERO real timer ticks for the entire window while the higher one is ready, identical across 3 repeated boots, full regression check clean against the complete pre-existing 117-assertion suite. |
| Context switch primitive (hand-written asm) | **Still flagged for human review** - not yet reviewed as of this note; built further on it anyway per the project owner's explicit instruction not to wait. Both schedulers above share it. |
| Interactive shell | Real (small). The `AgentOS>` prompt is IRQ1-driven (not polled), buffers a real line (heap-backed `String`) with backspace, Left/Right cursor movement, Shift (uppercase letters plus `~`/`-`/`_`), and Up/Down command history (a real `Vec<String>`), and dispatches `help` / `ps` / `mem` / `uptime` / `date` / `lspci` / `disk` / `ls` / `cat` / `touch` / `rm` / `mkdir` / `rmdir` / `clear` against the live scheduler, KV-cache, heap, timer-tick, real-time-clock, and real disk/filesystem state - not canned output. Insert/backspace both act at the cursor, not just at the end of the line - typing "hep", pressing Left once, then typing "l" correctly produces "help", not "hepl" or "lhep". Verified in QEMU: that exact sequence dispatches the real `help` command (its actual output appears in the log), not an "Unknown command" line. **Shift was a real, practical gap, not a nice-to-have**: before it, a real person at the keyboard could never actually type `cat KERNEL~1` themselves - no way to produce an uppercase letter or `~` at all - even though self-tests could dispatch it directly. Verified by feeding a realistic scancode sequence holding Left Shift across "KERNEL~" (letters plus the backtick key, which shifts to `~`) and releasing it before the unshifted "1": dispatches the real `cat` command a second time in the log (identical byte count to the direct self-test), not an "Unknown command". **`touch`/`rm` close a similar gap for file writes**: `Fat12Info::create_file`/`delete_file` (see the FAT12 rows below) existed for several Fases but were only ever reachable from hardcoded `main.rs` self-tests - a real person at the prompt had no way to create or delete a file themselves. `touch FILENAME [content...]` joins whatever's left on the line (single-spaced - this shell has no quoting, so exact original spacing isn't preserved) and creates a new file with it as content; `rm FILENAME` deletes one. Both are FAT12-only, matching `create_file`/`delete_file` themselves - a real FAT32 disk would get a clear "not implemented yet" error rather than a silent no-op, currently untested in practice since this disk is FAT12. Verified in QEMU through the *actual* `dispatch_command` parsing path (not by calling `Fat12Info` methods directly, unlike the tests that first proved `create_file`/`delete_file` themselves correct): `touch SHELLNEW.TXT hello from the real shell` creates a real 25-byte file, `cat` reads the exact same content back (first 4 bytes `68 65 6c 6c` - "hell", confirming genuine content, not just a matching length), `rm` deletes it, and a second `cat` then fails with "file not found" - identical across 3 repeated boots. |
| KV-cache memory manager | Real (small). Each block is a genuine heap allocation (`Box<[u8]>`, 4 KiB - sized for the current 1 MiB demo heap, not a realistic model-serving size yet); freeing a block really deallocates it. Previously used a fabricated address (`0x2000000 + idx*size`) that nothing was ever mapped to. |
| GGUF model loader / tensor engine | **The header, a real tensor-info entry, AND the tensor weight values it points to are now all real disk I/O, not compile-time constants; a real quantized format (Q8_0) can now be genuinely decoded too, not just parsed as metadata - the tensor math itself is still an honest toy.** **`GgufModelLoader::decode_q8_0` (Fase 50) implements GGML's actual `block_q8_0` format** - a 2-byte half-precision (binary16) scale followed by 32 signed 8-bit values per 34-byte block, each real value being `qs[i] as f32 * scale` - closing a real, previously-unnoticed gap: `GgufTensorInfo` has parsed and stored a tensor's real `gtype` since Fase 43, but nothing before this Fase ever branched on it to decide *how* to decode a tensor's bytes - every caller assumed `F32` regardless of what gtype it had actually parsed. A new `decode_tensor(bytes, gtype)` dispatcher finally makes that field meaningful, returning a clear "not implemented yet" error for the gtypes this Fase doesn't cover (`F16`/`Q4_0`/`Q4_1`) rather than silently misreading them as `F32`. Needed a real IEEE 754 half-precision (binary16) to `f32` conversion of its own - this `#![no_std]` build has no `f16` type or library to borrow one from - implemented and hand-verified against each of binary16's defined cases (zero, subnormal, normal, infinity) independently, the same "verify against the real standard, not just self-consistency" discipline the RFC 1071 Internet checksum work (below) used, given a wrong exponent bias or subnormal-handling bug would silently produce a plausible-looking but wrong float rather than an error. Verified with hand-computed bit-pattern test vectors (`0x3C00`→1.0, `0xC000`→-2.0, `0x0000`→0.0, `0x0001`→the smallest subnormal, 2⁻²⁴ - independently constructed from its own bit pattern, not a runtime `powi` call, which isn't even available without libm in this build) plus a full 32-value Q8_0 block decode checked against directly computing `value × scale` on the original signed bytes - all checks (`f16_vectors_ok`/`q8_0_decode_ok`/`dispatch_f32_ok`/`dispatch_q8_0_ok`, plus a `dispatch_unimplemented_ok` proving `F16` honestly returned `Err` at the time - retired once Fase 54 gave `F16` a real decoder too, since there was no longer any gtype left to exercise that path with) passing identically across 3 repeated boots, zero regression across the entire prior self-test suite. Deliberately disk-independent, pure byte-transformation logic (unlike the disk-load test below) - the disk-loading mechanics were already proven in Fase 41-43, so this Fase is scoped purely to the quantization math. **`GgufTensorInfo::parse_many` (Fase 51) generalizes tensor-info parsing to any real number of tensors, not just one** - `parse` has returned "how many bytes this one entry consumed" since Fase 43 specifically so several could be parsed in sequence, but nothing before this Fase ever actually looped over more than one; `parse_many` builds on that existing return value without changing `parse` itself at all. Verified with a constructed buffer holding two tensor-info entries back-to-back with genuinely different gtypes (`"weights"` F32, `"scale_bias"` Q8_0) followed by both tensors' real data: `parse_many_ok` confirms both entries' name/dimensions/gtype/offset are exactly right *and* that the total bytes consumed lands exactly at the tensor-info section's own end (proof each entry's length was tracked correctly across the whole sequence, not just the first); `tensor1_decode_ok`/`tensor2_decode_ok` then feed each parsed entry's *own* gtype into `decode_tensor` - a genuinely different claim than Fase 50's own dispatch test (which used hand-built structs), proving `parse_many`'s real output is directly usable by `decode_tensor`, not just structurally similar to what it expects. All three checks passed on the first attempt, identically across 3 repeated boots, zero regression across the entire prior self-test suite. **`GgufModelLoader::decode_q4_0` (Fase 52) implements GGML's actual `block_q4_0` format** - 4-bit, nibble-packed, the smallest of GGUF's quantized formats: 18-byte blocks (a 2-byte half-precision scale plus 16 bytes packing 32 signed 4-bit values, two per byte). Verified directly against GGML's real `dequantize_row_q4_0` source specifically because its packing turned out to be **split-half, not interleaved** - byte `j`'s low nibble is `value[j]` and its high nibble is `value[j+16]` (for `j` in `0..16`), not the equally-plausible-looking `value[2j]`/`value[2j+1]` guess a naive implementation might reach for. Getting this wrong would have silently scrambled every decoded tensor rather than erroring, exactly the class of bug this session's "verify against the real source, not memory" discipline exists to catch. The self-test's own 32-value block uses a deliberately asymmetric nibble pattern (low nibble ascending 0..15, high nibble descending 15..0) specifically so a low/high swap or an interleaved-packing bug would produce a completely different, easily-distinguishable wrong sequence rather than accidentally passing - both `q4_0_decode_ok` and the `decode_tensor` dispatch check passed identically on the first attempt across 3 repeated boots, zero regression. **`GgufModelLoader::decode_q4_1` (Fase 53) closes out the legacy quantization thread** - the same 4-bit, split-half nibble packing as Q4_0 (confirmed to genuinely carry over, not just assumed, via GGML's actual `dequantize_row_q4_1` source), but with a full affine dequantization instead of Q4_0's fixed symmetric range: each 20-byte `block_q4_1` adds a second half-precision field (`{ d: scale, m: minimum }`) before the same 16 bytes of packed nibbles, and each value is `nibble as f32 * scale + min` rather than `(nibble-8)*scale` - the extra `min` field lets Q4_1 represent asymmetric value distributions Q4_0's centered range can't. Verified with scale=0.5 and min=1.0 (deliberately distinct, nonzero values, so a bug conflating or dropping either field produces visibly wrong output) combined with the same asymmetric nibble pattern Q4_0's test used - both `q4_1_decode_ok` and the dispatch check passed identically on the first attempt across 3 repeated boots, zero regression. **`GgufModelLoader::decode_f16_le` (Fase 54) closes the loop: every `GgufGtype` this kernel recognizes now has a real decoder.** Unlike every quantized format above, `F16` has no block structure or shared scale at all - it's a plain, unquantized half-precision tensor, so each value is simply its own raw 2 bytes decoded via the already-verified `f16_to_f32` (built for the quantized formats' block scales, reused here directly rather than duplicated) - by far the smallest of these five Fases, since the hard part (a correct binary16 conversion) already existed. Verified with the same hand-checked bit patterns (`0x3C00`/`0xC000`/`0x3800` → `1.0`/`-2.0`/`0.5`) already proven correct as `f16_to_f32` inputs in Fase 50 - both `f16_decode_ok` and the dispatch check passed on the first attempt, identically across 3 repeated boots, zero regression. Retired the Q8_0 self-test's own `dispatch_unimplemented_ok` check in the same Fase (see above) - a real, deliberate consequence of completing gtype coverage, not a bug: that check's entire premise (an unimplemented gtype existing to test against) is gone now that all five do. **`GgufTensorInfo::parse_many` is now also proven through real FAT12 disk I/O (Fase 56), not just the synthetic in-memory buffer Fase 51 used** - the exact gap that Fase's own notes flagged as not yet closed. A genuine 2-tensor GGUF file (header + 2 tensor-info entries with deliberately different gtypes again - `"weights"` F32, `"scale_bias"` Q8_0 - plus both tensors' real data) is written to its own dedicated file (distinct from the original single-tensor disk-load test's `MODEL.BIN`, so the two tests can never interact), read back, and both tensors are parsed *and* decoded from the disk-read bytes rather than the in-memory buffer used to construct them - each tensor's offset field is relative to the full file, matching the convention the original disk-load test already established, not just the tensor-info section. All seven checks (`write_ok`/`round_trip_ok`/`header_ok`/`parse_many_disk_ok`/`tensor1_disk_decode_ok`/`tensor2_disk_decode_ok`/`cleanup_ok`) passed on the very first boot, identically across 3 repeated boots (including correct self-cleaning across repeat boots against the same unregenerated image), zero regression across the entire prior self-test suite - including the original single-tensor disk-load test, confirmed unaffected in the same boot log. **`GgufModelLoader::decode_q4_k` (Fase 57) implements GGML's "K-quant" super-block format** - structurally different from, and meaningfully more complex than, every legacy format above: each 144-byte block holds 256 values (not 32), split into 8 sub-blocks of 32 with their OWN 6-bit-packed scale *and* min each, rather than one shared scale for the whole block - the actual point of K-quants, letting different regions of a long run be quantized more accurately than a single scale ever could. Getting this right needed real, deliberate care beyond this thread's usual "fetch the source, verify a vector" pattern: an early research fetch of this exact struct's field sizes came back **wrong** (`scales[8]`/`qs[64]` instead of the real `scales[12]`/`qs[128]`) - caught only by noticing the fetched `get_scale_min_k4` function referenced byte indices (`q[11]`) that would be out-of-bounds for an 8-byte array, then cross-checking against GGML's own `K_SCALE_SIZE`/`QK_K` constants (fetched separately) to resolve the contradiction. Given a research tool had already been wrong twice for this one format, `get_scale_min_k4`'s own intricate 6-bit-packing logic - which sub-blocks' scales and mins share which bytes' spare bits - was independently hand-derived and verified with a *complete* round trip (encoding 16 chosen values into a 12-byte array by inverting the fetched formula, then decoding those exact bytes back through it) covering all 8 sub-blocks and both branches of its own `j < 4` split, before any Rust was written. Every value round-tripped exactly. The self-test re-runs that same hand-verified vector as compiled code (`get_scale_min_k4_ok`), then builds one full real 144-byte block with four distinct 32-byte chunks (`0x00`/`0xFF`/`0x73`/`0x21`) checked against 8 hand-computed constants covering all 8 sub-blocks' scale/min pairs and both nibble halves (`decode_q4_k_ok`), plus the usual dispatch check (`dispatch_q4_k_ok`) - all three passed correctly on the very first boot, identically across 3 repeated boots, zero regression across the entire prior self-test suite. **`GgufModelLoader::decode_q6_k` (Fase 58) implements another K-quant super-block format, structurally different from Q4_K in three checked-not-assumed ways**: its single super-block scale `d` comes LAST in the 210-byte struct rather than first (breaking the pattern every legacy format and Q4_K itself followed); the 6-bit quantized value is split across two separate byte arrays (`ql`, the low 4 bits, and `qh`, the high 2 bits) rather than packed into one nibble-pair array; and its 16 sub-block scales are plain signed `i8` bytes, not 6-bit-packed like Q4_K's - one full byte per scale, no bit-extraction helper needed, purely multiplicative (`d * scale * value`) rather than Q4_K's affine scale-and-min. The struct's fetched field sizes (`ql[128]`, `qh[64]`, `scales[16]`, `d`) were cross-checked against GGML's own `dequantize_row_q6_K` pointer arithmetic before being trusted - that function advances its three array pointers by 64/32/8 bytes per 128-value half-block over 2 half-blocks, totalling exactly 128/64/16 bytes, matching the struct - the same kind of internal-consistency check that caught Q4_K's wrong fetched sizes last Fase, applied proactively this time rather than after a fetch already came back wrong. The four-quadrant bit-extraction (each 128-value half-block's 32-iteration inner loop produces four values per iteration, all four sharing one `qh` byte split into four non-overlapping 2-bit fields, combined with a `ql` nibble and recentered by `-32`) was hand-traced with concrete byte values before any Rust was written, not trusted from the fetched source alone. The self-test uses a uniform `ql`/`qh` byte pattern (so the four extracted values stay constant across every loop iteration) paired with eight distinct prime scale values specifically chosen to exercise both branches of the sub-block index used to select a scale, for all four quadrants - `decode_q6_k_ok` and `dispatch_q6_k_ok` both passed on the very first boot, identically across 3 repeated boots, zero regression across the entire prior self-test suite, this time confirmed via a full line-by-line diff across all three boot logs rather than just a targeted grep - which also surfaced a pre-existing, already-understood, and unrelated boot-to-boot difference in the much older FAT12 `create_test`: `AGENTOS.TXT` is a deliberately permanent fixture file, so it correctly reports `created=false` "already exists" on any boot after the first against an unregenerated disk image, while still reading back its original content correctly every time - not a regression, just a property of disk-image persistence this stricter check happened to notice. **`GgufModelLoader::decode_q5_k` (Fase 59) implements a third K-quant format, structurally much closer to Q4_K than to Q6_K**: `d`/`dmin` come FIRST (like Q4_K, unlike Q6_K's scale-last layout), and its `scales[12]` are 6-bit-packed via the exact same `get_scale_min_k4` Q4_K already uses - reused completely unchanged, not re-derived, since GGML's own `dequantize_row_q5_K` calls that same function identically to `dequantize_row_q4_K`. The only genuinely new piece is a 5th bit per value: a 4-bit nibble from `qs[128]` (the same split-half packing every nibble format above uses) plus `+16` if a corresponding bit in `qh[32]` is set - an unsigned 0..31 range, no `-32`/`-8`-style recentering - dequantized affinely the same shape as Q4_K's own formula, just with a wider per-value range. The `qh` array itself never advances across a block's four 32-value sub-iterations (unlike Q6_K's two separately-advancing arrays) - instead, shifting bit-masks (`u1`/`u2`, each `<<=2` per iteration) pick a different single bit position out of the *same* 32 bytes each time, together covering all 8 bits of every `qh` byte across the four iterations - a genuinely different design from Q6_K's, checked explicitly rather than assumed to generalize. Hand-traced with concrete bytes (a `qh` byte `0xAA` decomposed into its 8 individual bits, combined with four distinct `qs` nibble-pair bytes) before writing any Rust. The self-test reuses Fase 57's own already-verified `get_scale_min_k4` scale/min constants unchanged (that function didn't change at all, so its proven round trip needed no re-deriving) paired with a uniform `qh` byte and four distinct uniform `qs` chunks, giving 8 predictable sub-block values checked against 8 hand-computed dequantized constants - `decode_q5_k_ok` and `dispatch_q5_k_ok` both passed on the very first boot, identically across 3 repeated boots, zero regression across the entire prior self-test suite (confirmed again via a full line-by-line diff across all three boot logs, surfacing only the same already-understood `AGENTOS.TXT`-persistence difference Fase 58 first identified, nothing new). **`GgufModelLoader::decode_q3_k` (Fase 60) implements a fourth K-quant format, structurally the most intricate yet**: 16 sub-blocks of 16 elements each (not 8 of 32 like every K-quant above), a signed 3-bit value (2 low bits from `qs` plus 1 high bit from `hmask`, recentered by `-4`), and - genuinely new - a scale-reconstruction scheme that is NOT `get_scale_min_k4`: `Q3_K` has no separate "min" field at all, just one signed 6-bit scale per sub-block, closer in spirit to `Q6_K`'s single-scale design. GGML's real `dequantize_row_q3_K` treats the 12-byte `scales` array as three 32-bit words and reconstructs a fourth via a SWAR (SIMD-within-a-register) bit trick - reduced here to a plain per-index formula for clarity, but NOT trusted from derivation alone: independently cross-checked by re-implementing GGML's exact packed-word logic in a scratch script and comparing outputs across 2000 random byte inputs - an exact match every time - before any of this Rust was written; the full value-extraction loop (the `qs`/`hmask`/shift/mask interplay) was verified the same way, against a scratch reimplementation, not just hand arithmetic. The self-test's scales were chosen via the verified formula's own inverse to span the full 0-63 range in one arithmetic sequence, paired with `hmask` set to inverse alternating patterns (`0xAA` then `0x55`, the exact bitwise inverse) across its two halves so every one of the 8 total shifting-mask positions differs between them, and a uniform `qs` byte chosen for the same convenient four-distinct-2-bit-fields property `Q6_K`'s own test already used - `decode_q3_k_ok` and `dispatch_q3_k_ok` both passed correctly on the very first boot (no bugs found this time, unlike Q4_K's fetch-error discovery - the extensive upfront computational cross-checking is the likely reason), identically across 3 repeated boots, zero regression across the entire prior self-test suite. **`GgufModelLoader::decode_q2_k` (Fase 61) implements a fifth K-quant format, and - after `Q3_K`'s SWAR scale trick - a welcome return to simplicity**: no bit-packing scheme to derive at all. Each of `scales[16]`'s bytes holds BOTH a sub-block's scale (low nibble) AND its min (high nibble) directly, unpacked - confirmed against GGML's real `dequantize_row_q2_K` rather than assumed from the format's name alone, given how easy an unverified assumption would have been to reach for right after `Q3_K` needed real bit-shuffling. The quantized value is a plain 2 bits, used directly and unsigned (no recentering, no companion high-bit array the way `Q3_K`'s `hmask`/`Q5_K`'s `qh` need), dequantized affinely - the same shape as `Q4_K`'s formula, just with a trivial nibble-split scale/min lookup instead of 6-bit-packed ones. The outer loop structure (an `n`/`j`/`is`/`shift` interplay, `d`/`dmin` LAST in the 84-byte struct like `Q3_K`/`Q6_K`) is structurally identical to `Q3_K`'s own, just without a second `hmask`-style array to track alongside it. Verified computationally rather than by hand-tracing alone, continuing the discipline `Q3_K` introduced: a scratch script implementing this exact loop, run against 16 distinct scale/min pairs (spanning the full 0-15 nibble range) paired with a uniform `qs` byte, confirmed the 16 expected dequantized values before any Rust was written. `decode_q2_k_ok` and `dispatch_q2_k_ok` both passed correctly on the very first boot, identically across 3 repeated boots, zero regression across the entire prior self-test suite - this likely completes the real, model-storage K-quant family (`Q2_K` through `Q6_K`); only `Q8_K` remains among GGML's defined K-quants, and it functions more as an internal quantized-matmul computation format than something models are actually stored as. **`decode_q8_k` (Fase 67) closes out `GgufGtype` entirely - the last defined K-quant, and, despite being numbered last, structurally the SIMPLEST of the whole family: confirmed against GGML's real `ggml-common.h` struct (`{d: f32, qs: [i8; 256], bsums: [i16; 16]}`, 292 bytes) rather than assumed from the format's name, revealing two details worth naming since both would be easy to get wrong by pattern-matching against earlier K-quants instead of checking this one's own layout - `d` is a plain 4-byte `f32` here, not the 2-byte `ggml_half`/f16 every other K-quant uses, and `qs` holds full SIGNED bytes with no bit-packing at all.** GGML's own comment explains why: this format exists purely to quantize float activations on the fly for fast dot products against already-quantized weights (`"only used for intermediate quantization and dot products"`), not to compress a model file for storage, so dequantization is correspondingly trivial - `value[i] = d * qs[i]` for all 256 values, no sub-block scale/min lookup, no bit-shifting, no recentering. `bsums[16]` (a precomputed sum-of-16 cache existing purely to speed up GGML's own SIMD dot-product kernels) is present in the struct but deliberately unused by this decoder, which only needs to reconstruct `f32` values, not replicate that optimization. Tested with `qs[i] = i as u8` across all 256 values - a genuinely exhaustive check unlike every earlier decoder's hand-picked sample set, since it exercises every possible signed-byte value exactly once (`0..127` stay positive, `128..255` wrap to `-128..-1` when reinterpreted as `i8`) - paired with `d=2.0` (not `1.0`) so a bug that forgot the delta multiply, or that misread `d` as a 2-byte f16 instead of its real 4-byte f32, would produce visibly wrong output. Passed on the very first attempt, identical across 3 repeated boots, zero regression across the entire prior self-test suite. **Every `GgufGtype` this kernel recognizes now has a real decoder - `decode_tensor` has no unimplemented gtype left.** |

The rest of this thread, built up over three earlier Fases: first the 24-byte header round-tripped through a real file (`MODEL.BIN`) via `create_file`/`read_file`; then the same file grew 64 bytes of raw little-endian `f32` weight data, decoded back via a new `GgufModelLoader::decode_f32_le`. This Fase adds the piece both left out: a real `GgufTensorInfo::parse` between the header and the weight bytes - a length-prefixed name, two dimensions, a quantization type, and a byte **offset** to that tensor's own data elsewhere in the file - and the weight bytes are now located by *following that parsed offset*, not assumed to sit immediately after a fixed-size header. Deliberately still a simplified, honest slice of real GGUF, not a compliance claim: `u32` fields throughout instead of GGUF's `u64` (more range than this kernel's own toy tensors will ever need), exactly one tensor-info entry rather than a general `tensor_count` of them (the header's own declared count is set to `1` now specifically so it matches what's actually present - no longer a cosmetic unused number), and no KV-metadata section (`kv_count` honestly set to `0`, since none is written or parsed). Loading *real quantized* tensor formats (the actual point of GGUF as a format) remains separate, larger scope. Verified the full round trip (write, read back, byte-for-byte match, delete), that the parsed tensor-info entry's name/dimensions/type/offset all match what was written, that following the parsed offset and decoding *those* bytes reproduces the original weight values exactly (a lossless byte round trip, safe to assert bit-for-bit), and that running the matmul on those offset-located, disk-decoded weights produces the same known-correct output the original hardcoded array always did - all six checks passed identically across 3 repeated boots, zero regression across the entire FAT12/shell/VFAT self-test suite in the same boot log. |
| PCI bus enumeration | Real. `pci.rs` reads real config space via the legacy `CONFIG_ADDRESS`/`CONFIG_DATA` I/O ports (read-only - never reconfigures anything) and the `lspci` shell command lists what it finds. Verified against known-good hardware IDs, not just "didn't crash": QEMU's default machine reports exactly the expected i440FX host bridge (`8086:1237`), PIIX3/4 ISA/IDE/ACPI bridges (`8086:7000`/`7010`/`7113`), standard VGA (`1234:1111`), and an e1000 NIC (`8086:100e`) - all real, recognizable vendor:device IDs, not fabricated ones. Only scans bus 0 (no bridge recursion yet). |
| Real-time clock (CMOS RTC) | Real. `rtc.rs` reads the actual wall-clock date/time via the legacy CMOS I/O ports (0x70 index/0x71 data) - the `date` shell command shows it. Waits out the chip's Update-In-Progress flag first (a real, if uncommon, torn-read hazard if ignored) and checks Status Register B rather than assuming BCD vs. binary or 12h vs. 24h mode, since real hardware (and QEMU's emulation) can be configured either way. Verified about as concretely as a "real" check can be: QEMU syncs this clock to the host's actual wall-clock time by default, and the value read back during local testing matched the real date/time at the moment of the test, to the second. No century register exists reliably, so the year assumes 20xx. |
| Disk I/O (ATA PIO) | **Real, both read and write.** `ata.rs` reads/writes real 512-byte sectors on the primary ATA master via classic PIO (ports 0x1F0-0x1F7) - the `disk` shell command reads LBA 0 and checks for the `0x55AA` MBR boot signature, which is genuinely present since it's reading our own disk image. **Caught a real bug getting the read side working**: the drive raises IRQ14 on command completion regardless of whether anything is waiting on it, and this kernel had no handler for that vector at all - the first real read double-faulted the instant interrupts came back on after the operation. Fixed with a real (if minimal) `ata_primary_interrupt_handler` in `interrupts.rs`, not just by suppressing the symptom - and that same fix is why `write_sector` (added later, prerequisite for any future FAT write support) worked cleanly on the first real boot test, no repeat of that bug. `write_sector` follows the same PIO handshake as reads (select drive/LBA, wait for `DRQ`, transfer 256 words) plus a `CACHE FLUSH` (`0xE7`) afterward so a caller getting `Ok` back knows the write is genuinely committed, not just buffered. Verified with a real write-then-read-back test at a sector safely inside the FAT12 partition's unused tail, confirmed byte-for-byte identical across 3 repeated boots. **A genuine CI-only regression (caught right after Fase 64, not a VirtIO bug despite first appearing right after it) traced back to `poll_until`'s fixed 1,000,000-iteration spin-loop bound**: CI's boot-test step failed silently (a bare `grep -q` with no matching line prints nothing, and GitHub Actions runs `run:` steps under `bash -e`, so the whole step just stops - no error text at all beyond "Process completed with exit code 1"). Root-caused without guessing: pulled the run's real, structured, per-line-timestamped log (`gh run view --log-failed`, not the terminal-rendered version used earlier, which interleaves script-source echo with actual output); diffed the live QEMU stream against the script's own explicit `cat boot.log` dump to first confirm the captured content was genuine and untruncated (identical, modulo one stderr-only QEMU line never piped into `tee`); then extracted the CI YAML's own bash script verbatim into a standalone file and ran *that exact script* locally with `bash -x -e` against the confirmed-real log content - reproducing the identical silent exit 1 and, via the trace, pinpointing the precise failing line. The real log read `[FAT12] write_test restore_failed: ATA read: timed out waiting for the drive to respond` where `restored=true` was expected: restoring `BOOT-S~2` (152136 bytes, ~297 sectors - the single most poll-intensive operation in the entire self-test suite) exhausted `poll_until`'s retry budget. The bound was never wall-clock-based to begin with, just an iteration count sized for bare-metal port timing; under QEMU's software (TCG) port-I/O emulation each `in`/`out` costs a full VM-exit and device-model dispatch, so the same count buys far less real headroom on a loaded, shared CI runner - and this run was the first to need more of that margin than the old bound provided (plausibly related to the CI boot command now also carrying `-device virtio-net-pci`, adding one more emulated device for QEMU's main loop to service throughout the whole boot, though the exact contribution wasn't isolated). Fixed by raising `MAX_POLL_ITERATIONS` to 50,000,000 - free in the healthy path, since every poll still exits on the very first successful check; it only changes how long a genuinely absent/broken drive takes to report an error. Also fixed a real, adjacent bug surfaced during this same investigation: `poll_until`'s timeout error was hardcoded to always say "ATA read", even when called from `write_sector_inner` - exactly why this looked like a contradiction at first (a "write_test" reporting an "ATA read" timeout) - now parameterized so read/write failures are labeled correctly. Confirmed fixed across 3 repeated local boots (`restored=true` and the following `create_test` both succeed consistently) before pushing. |
| Partition table (MBR) | Real. `partition.rs` parses the 4 primary partition entries out of the boot sector already read via ATA. On our own disk image this finds two real bootable entries: a small internal region (the `bootloader` crate's own boot stages) and a partition typed `0x0C` ("FAT32 LBA") - **which turned out to actually be formatted FAT12**, see below. |
| FAT32 / FAT12 filesystem | **Real - read (`ls`/`cat`), overwrite-existing-file write, and now genuine new-file creation too, all verified end-to-end.** `fat32.rs` parses the BPB and can walk cluster chains / list a directory / read a file by name - **but our own disk's `0x0C`-typed partition is actually FAT12**, confirmed by reading the raw boot sector (`root_entry_count=512`, and the on-disk filesystem-type string literally reads `"FAT12   "`, volume label `"agentos-ker"`) - the `mbrman`/`fatfs` tooling that builds our disk image apparently doesn't always match the MBR type byte to the filesystem it actually formats for a volume this small. `shell.rs` tries FAT32 first and falls back to `fat12.rs` (packed 12-bit FAT entries, two per 3 bytes; fixed-location root directory instead of a cluster chain) when it doesn't apply - which is what makes both commands actually work on this disk. `ls` lists all 3 real files present (`BOOT-S~1`, `BOOT-S~2`, `KERNEL~1` - the `bootloader` crate's own two stages plus our kernel binary; each roughly 116-150 KiB, exact byte counts not pinned anywhere since they turned out to shift a little between build machines - see next paragraph). `cat KERNEL~1` reads the whole file back through a real cluster-chain walk (its length matching whatever `ls` reported that boot), and the first 4 bytes are always `7f 45 4c 46` - the genuine ELF magic number (`\x7fELF`), a build-independent invariant and about as strong a correctness proof as available without a full checksum. **Found the hard way (three times)**: first, the initial FAT32 code silently misread FAT12's differently-shaped BPB as FAT32 fields, computing a garbage LBA in the billions; fixed by checking `root_entry_count` (FAT32 always zeros it, FAT12/16 never do - the standard, authoritative discriminator) and failing with a clear, honest message instead of guessing. Second, actually `cat`-ing a real file panicked with a heap-allocation failure - every real file on this disk (116-148 KiB) turned out to be bigger than the entire 100 KiB heap that existed at the time, since `read_file` needs one contiguous allocation for the whole file; fixed by raising the heap to 1 MiB (see memory row above). Third, a CI regression check that hardcoded the exact byte count seen locally (`KERNEL~1 132992 bytes`) promptly failed on the Linux CI runner, which built a `KERNEL~1` of 132960 bytes from the identical source - the kernel ELF's own size isn't stable across build machines, so the CI check was rewritten to assert on the build-independent ELF magic bytes instead of a specific length. Directory-entry parsing (32-byte short-name entries, VFAT long-name entries recognized and skipped) is shared between both filesystem modules via `fat_common.rs`. **Fourth thing found the hard way**: `fat12::write_file` (new at the time - overwrote an existing file's content at the same byte length only, reusing its existing cluster chain with no directory/FAT changes; resizing came later, see the dedicated FAT12 file resize row below) was first tested by overwriting `BOOT-S~2` with a throwaway pattern, on the assumption that nothing in this kernel's own self-tests reads it so it was "safe." Wrong assumption, caught by a routine repeat-boot-test: `BOOT-S~1`/`BOOT-S~2` are the `bootloader` crate's own boot-stage *executables*, run by the BIOS before this kernel exists - QEMU's `-drive` writes straight through to the host `.img` file, so leaving that overwrite in place corrupted the boot chain for every subsequent boot against the same image (the first boot looked fine, since its own chain had already handed off to this kernel before the corruption happened; only the *next* boot attempt hit a totally empty serial log, no panic, because a corrupted stage 2 never got far enough to run any kernel code at all). Fixed by having the self-test unconditionally restore the original content afterward - confirmed safe across 3 repeated boots against the same unregenerated image. |
| FAT12 new-file creation | **Real - genuinely new files, not just overwriting existing ones - and now of any size, not just single-cluster.** `Fat12Info::create_file` finds as many free clusters as the data needs (`find_free_clusters`, one pass over the in-memory FAT collecting distinct `0x000` entries - calling a "find one free cluster" function repeatedly would just return the same cluster every time, since nothing marks one "taken" until its FAT entry is actually written) and a free root-directory slot (first byte `0x00`/`0xE5`), writes the data across all of them, chains each cluster to the next in the FAT (last one marked end-of-chain), and writes a real short-name (8.3) directory entry - including a genuine creation timestamp read from the CMOS RTC (Fase 20), not a zeroed placeholder. Short names only still (no VFAT long-name entries). Writing a 12-bit FAT entry correctly handles the one real edge case that format has: at specific cluster numbers (341, 682, 1023...) the packed entry straddles a 512-byte sector boundary, so both sectors are read/written when that happens rather than assumed never to occur. Verified in QEMU: creates `AGENTOS.TXT`, reads it back and confirms the content matches, and `ls` genuinely shows the entry count going from 3 to 4. Repeat boots against the same unregenerated image correctly detect the file already exists (no duplicate entry, no double-allocated cluster) and still read back the same real content - confirmed identical across 3 repeated boots, learning directly from the FAT12-write lesson above about what "safe to run every boot" actually requires. **Multi-cluster support (originally deferred - "several free-cluster lookups tracked against each other so the same free cluster is never handed out twice" was real, meaningfully more bookkeeping) verified separately**: creates a 3100-byte file (this disk's clusters are 1024 bytes each, so this genuinely needs 4 chained clusters, not 1), reads it back byte-for-byte against a deterministic pattern (not just a matching length), and deletes it - which also happens to be the first real test of `delete_file`'s cluster-chain walk against an actual multi-cluster chain (Fase 27 only ever proved that logic against single-cluster files, though it was written generally from the start). All correct, identical across 3 repeated boots. |
| FAT12 file deletion | **Real - frees both the cluster chain and the directory slot, not just a cosmetic marker.** `Fat12Info::delete_file` finds the entry's exact on-disk location (`find_entry_location`, which tracks slot index unlike `list_root_directory`), walks its full cluster chain zeroing each FAT entry back to `0x000`, and marks the directory entry's first byte `0xE5` (the standard FAT convention `find_free_root_entry` already knew to treat as reusable). **Found the hard way, a genuine cache-coherence bug**: the very first test run failed with `cluster chain hit a free (0) entry unexpectedly`, even though the file had been created moments earlier in that same boot. Root cause: `write_fat_entry_to_disk` (used by both `create_file` and `delete_file`) wrote the new FAT value straight to disk but never updated the in-memory `fat_bytes` copy loaded once at mount time - so any later call within the same `Fat12Info` that consulted the cache (`next_cluster`, `find_free_cluster`) saw stale, pre-write data. This had been silently latent since `create_file` was added: `read_file` never surfaced it because its read loop returns as soon as it has enough bytes, so a single-cluster file never reaches its own `next_cluster` call - `delete_file` is the first caller that walks the chain unconditionally, regardless of file size, which is exactly why it's the one that exposed a bug that had been sitting there since the previous Fase. Fixed by having `write_fat_entry_to_disk` write the same two bytes into `fat_bytes` right after the disk write succeeds, keeping the cache coherent for the rest of that mount's lifetime. Verified in QEMU: creates a dedicated `DELETEME.TXT` (kept separate from the already-verified `AGENTOS.TXT`), confirms it exists, deletes it, then confirms `read_file` now fails and `ls`'s entry count drops back down - correct across 3 repeated boots against the same unregenerated image, including boots where the file first had to be freshly re-created (since the previous boot's delete had already removed it from disk). |
| FAT12 file resize (grow + shrink) | **Real - `write_file` can now change a file's size, not just overwrite it at the same length.** Originally same-size-only ("resizing needs FAT chain growth/shrink... substantially more work, deliberately not attempted") - closed once multi-cluster `create_file` (previous row) had already built the one piece resizing needed too: safely allocating (or, for shrinking, freeing) several clusters at once. `write_file` now walks the file's existing chain, computes how many clusters the new data needs, extends the chain with `find_free_clusters` if growing or frees the excess trailing clusters if shrinking, writes the data across the (possibly resized) chain, and patches the directory entry's size field on disk - `start_cluster` never changes, since the first cluster of an existing file's chain never does. Verified in QEMU with a single dedicated file taken through all three states in one boot: created at 500 bytes, grown to 2500 (needing 3 clusters on this disk's 1024-byte clusters, i.e. genuinely allocating 2 *new* ones and extending the chain), then shrunk to 300 (freeing those same 2 clusters back to the FAT) - each phase checked byte-for-byte against a *different* deterministic pattern (different modulus, different length) specifically so a bug leaving stale bytes from an earlier phase in place would show up as a mismatch rather than an accidental false pass. All three phases correct, plus the file is deleted afterward to keep the test self-cleaning - identical across 3 repeated boots. The original same-size overwrite test (`BOOT-S~2`, previous section) still passes unchanged, confirming the generalization didn't regress the simplest case. |
| FAT12 subdirectories (create, list, delete, nested) | **Real - `mkdir` creates a genuine walkable subdirectory, `ls DIRNAME` lists it, `rmdir` deletes it.** Unlike the fixed-location root, a subdirectory is just an ordinary single cluster holding real `.` (points to itself) and `..` (points to its parent - cluster `0` is the real FAT convention meaning "the parent is the root", since the root has no cluster number of its own to point back to) entries, marked `ATTR_DIRECTORY` in its own root-directory entry. **A real correctness point, handled deliberately rather than assumed**: a newly-allocated cluster's FAT entry being `0x000` (free) says nothing about what's still sitting in its actual *data* bytes on disk - this kernel's own self-tests create-then-delete several files every single boot, so a fresh cluster can easily still hold a previous file's leftover content. That's harmless for a plain file (only bytes up to its logical size are ever read back), but a directory's bytes are always parsed as structured entries - stale data past `.`/`..` could be misread as bogus additional entries. `create_directory` explicitly zeroes every sector of the new cluster beyond the first, not just the one holding `.`/`..`. Verified in QEMU: `mkdir TESTDIR` then `ls` shows it in the root as a real directory (`dir=true`, a genuine cluster number), and `ls TESTDIR` shows **exactly 2 entries** - `.` (pointing back to its own cluster) and `..` (cluster `0`) - proving the zeroing worked, not just that the two real entries happen to also be present alongside undetected garbage. `TESTDIR` deliberately persists across repeat boots (like `AGENTOS.TXT` does) to keep exercising `mkdir`'s "already exists" tolerance path. `rmdir` (added once `mkdir`/`ls` were proven solid) frees the directory's cluster and marks its root entry deleted - the same mechanics `delete_file` already uses - and refuses to delete a non-empty directory (checked via its own `.`/`..`-only content), the standard safe `rmdir` semantic; verified with a separate, fully self-cleaning `mkdir`→`rmdir`→confirm-gone round-trip each boot. File I/O *inside* a subdirectory (not just root) is now real too - see the dedicated row below. **Directories can now nest one level deeper too**: `create_directory`/`delete_directory` were generalized the exact same low-risk way as the file-I/O row below - an internal `DirLocation` parent parameter, existing functions kept as unchanged-signature wrappers, new `create_directory_in`/`delete_directory_in` for a directory whose parent is itself a subdirectory rather than root. The one genuinely different piece for a nested directory: its `..` entry must point to the *parent's real cluster*, not always `0` - `0` is specifically the "parent is the root" convention, which no longer holds once a directory's parent is itself another directory. Verified in QEMU: creates an outer directory, creates an inner one nested inside it, confirms the outer directory's listing shows exactly 3 entries (`.`, `..`, and the inner directory - no stale garbage), and - the real correctness check - confirms the inner directory's `..` entry's cluster number **equals the outer directory's own cluster**, not `0`. All four checks (`outer_count_ok`/`dotdot_ok`/`delete_inner_ok`/`cleanup_ok`) correct on the first attempt, zero regression across all eight prior FAT12 self-test suites confirmed in the same boot log, correct across 3 repeated boots. Not yet exposed via shell path syntax (that needs multi-segment path parsing, e.g. `A/B/file.txt` - a separate concern) - proven at the `Fat12Info` API level first, same order as everything else in this arc. |
| FAT12 file I/O inside a subdirectory | **Real - `create_file`/`read_file`/`write_file`/`delete_file` can now target a subdirectory, not just the root.** Previously the single biggest remaining FAT12 gap, flagged as future work across several prior Fases. Closed with a deliberately low-risk, additive design rather than changing any existing function: a small internal `DirLocation` enum (`Root` or `Cluster(u32)`) plus private `*_impl` functions holding the real logic - the existing `create_file`/`read_file`/`write_file`/`delete_file` became thin, **signature-unchanged** wrappers calling their `_impl` with `DirLocation::Root`, and new `create_file_in`/`read_file_in`/`write_file_in`/`delete_file_in` functions (taking a directory's cluster number) call the same `_impl` with `DirLocation::Cluster(...)`. `find_entry_location`/`find_free_root_entry` were generalized the same way, via a new shared `directory_sectors` helper that flattens "which sectors make up this directory" (the root's fixed range, or a subdirectory's cluster chain walked to completion) into one list every entry-scanning function iterates identically either way. This needed **zero new low-level disk-I/O code** - multi-cluster allocation (`find_free_clusters`), resize (`write_file`'s grow/shrink), and cluster-chain listing (`list_directory`) already existed from earlier Fases and slotted in directly. Deliberately does **not** grow a subdirectory's cluster chain if it fills up (same single-cluster limitation `create_directory` already has). **Verified with real care for regressions**, since this touched four already-extensively-tested functions: the one exact error string CI already asserts (`"FAT12: file not found in root directory"`, from `cat`'s failure path) was deliberately preserved byte-for-byte for the root case (a `match`-selected message keeps the subdirectory case honestly distinct: `"...in directory"`). Verified in QEMU: creates a dedicated subdirectory, creates a file inside it, reads it back matching, grows it via `write_file_in` (1500 bytes, needing multiple clusters), reads that back matching too, deletes the file, confirms it's gone, then deletes the now-empty subdirectory itself - all four steps genuinely correct on the very first attempt. Every one of the five pre-existing FAT12 self-test suites (overwrite, create, delete, multi-cluster, resize) plus all shell-level tests (`touch`/`rm`/`cat`/`mkdir`/`rmdir`) confirmed passing identically, unchanged, in the same boot log - real, checked evidence of zero regression, not just an assumption from the additive design. Correct across 3 repeated boots. **`touch`/`cat`/`rm` now understand a `DIR/FILE.EXT` path too** (one level deep, matching what `Fat12Info` itself supports) - `shell.rs` splits it and resolves `DIR` to its cluster before calling the `_in` functions above, falling back to the existing plain-name/root behavior unchanged when there's no `/`. Verified through the real interactive `dispatch_command` parsing path (not calling `Fat12Info` directly): `touch PATHTEST/INSIDE.TXT hello via a real path` creates a real 21-byte file inside a fresh subdirectory, `cat` reads it back matching (first 4 bytes `68 65 6c 6c` - "hell"), `rm` deletes it, a second `cat` correctly fails with the subdirectory-specific "not found in directory" message (not the root one - proof the path genuinely routed to the right place), and `rmdir` removes the now-empty test directory - fully self-cleaning, identical across 3 repeated boots. |
| FAT12 subdirectory growth (multi-cluster directories) | **Real - a subdirectory can now grow past its first cluster once its entry slots run out**, instead of `create_file_in`/`create_directory_in` simply failing with "directory is full" the moment a subdirectory's one starting cluster (32 entries on this disk, 2 of which are always `.`/`..`) is exhausted. `find_free_entry_in` now falls back to `grow_directory` when no free slot exists in a subdirectory's current chain: walks to the chain's real tail, allocates and zeroes a fresh cluster (the same "stale bytes are dangerous for directories" zeroing `create_directory` already does), and chains it on - reusing the exact allocate-then-chain primitives `create_file`/`write_file` already use for a *file's* chain, since growing a directory's chain isn't fundamentally different, just applied to entries instead of file bytes. The root directory itself still can't grow (a real, permanent limit - its size is fixed by the BPB, not a cluster chain, so a full root is a genuine error, not a gap). **A real bug found by this Fase's own self-test, not by chance**: the first attempt correctly created all 31 test files and reported the right final entry count, but the *last* file's content failed to read back correctly and the test directory couldn't be deleted afterward - both symptoms of silent data corruption. Root cause: `create_file_impl`/`create_directory_impl` both picked a free cluster for their own new content *before* asking for a free directory-entry slot - but `find_free_clusters` only reads the in-memory FAT cache, it doesn't reserve anything until a later write actually commits it. When the entry-slot search subsequently triggered `grow_directory` (which *does* immediately commit its own new cluster), it could - and, once actually exercised at scale, did - pick the exact same "free" cluster the file/directory's own content had already been assigned moments earlier, since neither allocation had told the other about it yet. Fixed by reordering both functions: find (and, if needed, grow into) the entry slot *first*, so any directory growth is committed to the FAT before the new file/directory's own cluster search ever runs. Verified with a dedicated test creating 31 one-byte files inside a fresh subdirectory (this disk's exact per-cluster capacity, so the 31st genuinely requires a second cluster to exist at all) - all created, the directory listing showing exactly 33 entries (`.`, `..`, 31 files), the 31st file's content read back correctly (the single strongest proof the new cluster is genuinely functional storage, not just "created without erroring"), all 31 deleted, and the directory itself removed afterward - all five checks correct after the fix, identical across 3 repeated boots, with zero regression confirmed across all nine other FAT12 self-test suites in the same boot log. |
| FAT12 long file names (VFAT) | **Real - genuine long file names (spaces, mixed case, anything past what 8.3 allows, up to 195 characters), not just a display cosmetic.** `create_file`/`create_directory` fall back to generating a real VFAT sequence - one short-name alias entry plus as many long-name entries (attribute `0x0F`) as the name needs - whenever a name doesn't fit plain 8.3; the short-name checksum every long entry carries came straight from the Linux kernel's own `Documentation/filesystems/vfat.txt` rather than reimplemented from memory, given how easy that exact kind of bit-twiddling is to get subtly wrong. The read side (`fat_common::parse_dir_sector`, shared with `fat32.rs`) reconstructs the long name - checksum-verified against the short entry it describes - for both `ls` display AND direct lookup: `cat`/`rm`/a resize via `write_file` all work by *either* the real long name or the generated short alias, not just whichever one `ls` happens to show, since `find_entry_location_in` shares the identical reconstruction state machine (`LfnState`) with the display path. Originally shipped capped at a single 13-character long-name entry, then extended (same Fase arc, one iteration later) to `build_lfn_entries` chaining up to 15 of them - 195 characters - the largest that still keeps a new entry's required slots (long entries plus the one short entry) within its own deliberate single-sector limit (16 entries; the search simply continues into the next sector rather than splitting a slot run across both, and a bounds check added alongside the multi-chunk extension rejects any `count` over 16 outright rather than risk it silently computing a byte offset past a single sector buffer). **Two real bugs found and fixed by the single-chunk self-test, not by inspection**: a plain 8.3 name typed directly skipped all collision checking against an already-generated VFAT alias, so two directory entries could end up sharing one identical short name, corrupting whichever lookup found the wrong one first - fixed by checking `existing_short_names_in` on that path too, not only when an alias is freshly generated; and `read_file` and `delete_file`/`write_file` used two entirely separate matching paths that silently disagreed - reading found a VFAT file only by its long name, deleting only by its short alias - fixed by unifying `read_file` onto the same `find_entry_location_in` the others already used, now trying both names. Verified end-to-end at both boundaries: a 13-character single-chunk name with an embedded space (`ls` shows the real name, reading back succeeds by *both* the long name and the alias, a colliding plain-alias create is correctly rejected, delete and confirm gone by both names - repeated for a directory, deleted by its long name specifically to exercise both lookup directions) and, one iteration later, a 32-character name needing 3 chained long entries (created, shown, read back correctly by *both* names again - proving the multi-chunk reconstruction on the read side, general from the start but never previously exercised past one chunk, actually works - then deleted by its long name to exercise `find_entry_location_in`'s own multi-chunk accumulation specifically). All checks passed identically across 3 repeated boots each time, with zero regression across every other FAT12 self-test suite in the same boot log. |
| Multi-segment shell paths | **Real - `touch`/`cat`/`rm`/`mkdir`/`rmdir`/`ls` all understand `DIR1/DIR2/.../NAME` now, any number of levels deep, not just one.** `Fat12Info`'s own nested-subdirectory support (`DirLocation::Cluster`) has had no depth limit since it was first generalized - only `shell.rs`'s own path parsing did, hard-rejecting a second `/` and never splitting a path at all for `mkdir`/`rmdir`/`ls`. Replaced the old one-level `split_path` with `split_segments` (validates and splits every `/`-separated piece) and a new `resolve_dir_path` that walks an arbitrary list of directory names from the root to the innermost one's cluster, reusing the exact `list_root_directory`/`list_directory` calls already proven correct by every earlier FAT12 Fase. `mkdir`/`rmdir` reuse the same parent-chain-plus-final-component split file paths use (`mkdir A/B` creates `B` inside already-existing `A` - matching plain `mkdir`'s usual behavior, not `-p`: a missing parent segment is a real, honest error); `ls DIR1/DIR2` resolves the *whole* path to the directory being listed instead. This is the first time a person at the `AgentOS>` prompt can create, populate, and list a nested directory at all - Fase 36 proved nesting worked at the `Fat12Info` API level, but nothing before this exposed it interactively. Verified through the real `dispatch_command` path (not calling `Fat12Info` directly): `mkdir MULTIOUT` → `mkdir MULTIOUT/MULTIIN` (a directory created *inside* another one by name, from the shell, for the first time) → `touch`/`cat` a file two levels deep → `ls MULTIOUT/MULTIIN` shows its 3 entries → `rm` the file → a second `cat` correctly fails with **`"FAT12: file not found in directory"`** specifically, proof the path genuinely routed two levels down rather than coincidentally failing some other way → `rmdir MULTIOUT/MULTIIN` then `rmdir MULTIOUT` clean up. All nine steps correct on the first attempt, identical across 3 repeated boots, zero regression across every other FAT12/shell self-test in the same boot log. |
| VirtIO-net / TCP/IP stack | `net::virtio_net` (the TCP/IP-facing driver) remains fully simulated - fixed MAC, packet counters, no real hardware. **A new, separate `net::virtio` module (Fase 62) now reaches a real second NIC** - see its own paragraph at the end of this row. **`net::e1000.rs` reaches the real NIC, and TX is now fully confirmed working end-to-end.** Finds the device via PCI (vendor `0x8086`/device `0x100e`, confirmed present at 00:03.0 by real enumeration), reads its real BAR0, and reads back real MMIO registers - no new page-table mapping code needed, since this kernel already identity-maps all physical memory at a fixed offset for the VGA text buffer, and that mechanism turned out to cover PCI MMIO space too. The MAC read from the real RAL0/RAH0 registers is `52:54:00:12:34:56` - QEMU's well-known default NIC MAC, now read for real off actual hardware. A real transmit descriptor ring is built (fresh physical frames via `memory::frame_allocator`, Fase 21), a real ARP request is written into it, the hardware dequeues it (`TDH` advances), and - as of Fase 44 - **the descriptor's Descriptor-Done status bit genuinely gets written back too**, confirmed by direct register read-back (`dd=true`), identically across 3 repeated boots. **A real, two-round mystery (Fase 22, Fase 32), resolved by investigating something else entirely**: every prior round correctly ruled out wrong register offsets, `TIPG`/`CTRL.SLU` misconfiguration, a starved poll loop, CPU cache incoherency, and deferred completion - each with its own real experiment, none of them the actual cause. The real answer surfaced while researching real RX support (see below): QEMU's `hw/pci/pci.c` only enables the memory region every `pci_dma_read`/`pci_dma_write` call routes through (`bus_master_enable_region`) once the guest sets the PCI Command register's Bus Master Enable bit - a real, standard PCI/PCIe requirement (a freshly enumerated device starts with bus mastering *disabled*, a genuine safety property), not a QEMU quirk. This kernel had simply never done it - `pci.rs` was read-only by design until Fase 44 added `write_config_word`/`PciDevice::enable_bus_mastering`. That single call, and nothing else, made `DD` start appearing: `TDH` is an internal register update (unaffected by bus mastering, hence always correct), while `DD` is a real DMA write to guest memory (silently dropped without it) - precisely the asymmetry both investigation rounds observed but couldn't explain from inside this driver's own, already-correct code. See `net/e1000.rs`'s module doc for the full two-round investigation and its resolution. **RX is now real too (Fase 45), and it genuinely received a real reply from the network on the very first attempt.** `receive_test_frame` arms a real receive descriptor ring (register offsets/`RCTL` bits/descriptor layout verified against QEMU's actual source, same discipline as the TX investigation) *before* triggering `send_test_frame`'s ARP request, then polls for a reply - and QEMU's SLIRP backend (the default user-mode network gateway at `10.0.2.2`) answered it: a genuine 64-byte Ethernet frame arrives, addressed to this kernel's own MAC, EtherType ARP. Verified as a real reply, not a same-shaped coincidence: the ARP `OPER` field is `0x0002` (reply, not request), the Sender Hardware Address matches the Ethernet source MAC (internal cross-consistency a malformed frame wouldn't have), and the Sender Protocol Address is `10.0.2.2` - *exactly* the IP the original request asked about. Identical, byte-for-byte, across 3 repeated boots. A new `netcheck` shell command (Fase 46) exposes this same TX+RX round trip interactively, on demand, rather than only ever at boot - zero changes to `net::e1000` itself, since `receive_test_frame` was already proven correct and is reused as-is. **A new `arp_resolve(target_ip)` (Fase 48) generalizes that same TX+RX round trip to resolve *any* target IP's MAC address**, not just SLIRP's hardcoded gateway - the direct prerequisite a real `ping` (see the IPv4 + ICMP protocol layer row below) needs before it can address an Ethernet frame at all, since sending an IP packet requires the destination's MAC first. Deliberately a new, independent function rather than a refactor of `send_test_frame`/`receive_test_frame` - their exact behavior and log output are CI-asserted across three Fases (44-46), so this duplicates their proven ring-setup shape (same register constants, same descriptor structs, same bounded poll pattern) instead of risking any change to them. Worked correctly on the very first attempt, resolving SLIRP's gateway (`10.0.2.2`) to exactly `52:55:0a:00:02:02` - the same MAC Fase 45's own RX test already observed as the replying device's source address, giving a precise, exact correctness check rather than just "got 6 bytes back from somewhere". Identical across 3 repeated boots. **A new `net::e1000::ping(target_ip)` and matching `ping A.B.C.D` shell command (Fase 49) converge `arp_resolve` and the IPv4 + ICMP protocol layer (below) into the first complete, real, routable IP packet this kernel has ever assembled and sent** - not just a raw Ethernet-level test frame. Resolves the destination MAC, builds a full Ethernet + IPv4 + ICMP-echo-request frame via `net::icmp`'s builders, sends it through a new (again independent of `send_test_frame`/`receive_test_frame`) TX/RX round trip, and verifies a genuine echo reply: EtherType IPv4, IPv4 protocol ICMP with a valid header checksum, source address matching the host being pinged, and an ICMP echo reply (not request) whose identifier/sequence match what was sent. **Honestly uncertain going in - SLIRP was confirmed to answer ARP for its own gateway, but never yet asked to answer a ping - and it worked on the very first attempt**, identically across 3 repeated boots (`rtt_ticks=19`, about 1 second at this kernel's ~18.2Hz timer rate), both from a direct call and through the real `dispatch_command` shell parsing path. **`net::virtio` (Fase 62) is a genuinely different, second real NIC driver, deliberately kept separate from `net::e1000.rs` rather than merged into it - first stage only, mirroring `net::e1000`'s own Fase 19 opening: find the device, reach its registers, read a few back, no virtqueues/TX/RX yet.** QEMU's default machine has no VirtIO device at all - this kernel's boot command (`boot_kernel.bat` and CI's own QEMU invocation) now adds `-device virtio-net-pci`, **verified empirically before writing any code, not assumed**: the new device lands at PCI slot `00:04.0`, confirmed to leave the already-present e1000 at `00:03.0` (and every other existing device) completely unaffected, so every prior PCI/e1000 self-test and CI check stays valid unchanged. **Genuinely different from e1000 in the one way that matters most just to reach its registers at all**: e1000's BAR0 is memory-mapped; VirtIO's legacy PCI transport's BAR0 is I/O-port space instead - confirmed by reading the real BAR0 value back and checking bit 0 (1 = I/O space), needing genuine `in`/`out` port instructions (`x86_64::instructions::port::Port`, the same primitive `pci.rs`'s own `CONFIG_ADDRESS`/`CONFIG_DATA` access already uses) rather than a `read_volatile` through a mapped address. Register offsets were verified against the Linux kernel's own real, canonical `include/uapi/linux/virtio_pci.h` and `include/uapi/linux/virtio_net.h` headers - fetched directly rather than assumed from a paraphrased description - and which of two possible device-config offsets (20 without MSI-X, 24 with) actually applies for this exact QEMU instantiation was confirmed empirically (both were read back and compared locally) rather than assumed either way. Verified with a real, plausible result: `features=0x79bf8064` (a genuine non-trivial bitmask, bit 5/`VIRTIO_NET_F_MAC` correctly set), and a MAC of `52:54:00:12:34:57` - one past e1000's own `52:54:00:12:34:56`, exactly QEMU's default per-NIC MAC-assignment scheme for this device list, strong evidence this is genuine device data, not garbage - identical across 3 repeated boots, zero regression across the entire prior self-test suite (including every e1000/PCI check, confirmed completely unaffected by the new device in the same boot log). **`net::virtio::init_tx_queue` (Fase 63) completes the real legacy VirtIO device-initialization handshake (`ACKNOWLEDGE` -> `DRIVER` -> `DRIVER_OK`, declining every optional feature) and sets up ONE real virtqueue - the TX queue, index 1 by virtio-net's own convention - still no descriptor built or frame sent yet, mirroring `net::e1000`'s own next-stage-after-probe shape (its Fase 22 TX descriptor ring), the same multi-Fase progression a working TX needed there too.** `QUEUE_NUM` is read back from the device rather than assumed (confirmed empirically at 256 for this QEMU version) - the legacy interface gives the driver no way to negotiate a smaller virtqueue, so the full byte size a real 256-entry vring needs (verified against the Linux kernel's own real `vring_size()` formula from `include/uapi/linux/virtio_ring.h`: 10246 bytes) must be allocated exactly, needing 3 physical frames, not 1 - **the first time this kernel's frame allocator has needed to hand back genuinely *contiguous* memory across several calls**, a real, different requirement from every e1000 ring (which always fit in a single frame). Rather than silently assume sequential `allocate_frame()` calls stay contiguous (true today, per Fase 21's own self-test, but an emergent property of the current bump-allocator implementation, not a documented guarantee), `allocate_contiguous_frames` checks explicitly and returns a clear error if a future allocator change ever broke that assumption. Verified with a genuinely convincing result on the very first attempt: `queue_num=256`, `frames_needed=3` (both matching the calculation exactly), `pfn_ok=true` (the device's own `QUEUE_PFN` register reads back *exactly* what was written - real proof of acceptance, not just "the write didn't crash"), and `final_status=0x07` (`ACKNOWLEDGE|DRIVER|DRIVER_OK`, confirming the full handshake stuck and is readable back) - identical across 3 repeated boots, zero regression across the entire prior self-test suite. **`net::virtio::send_test_frame` (Fase 64) builds a real 10-byte legacy `virtio_net_hdr` (all-zero: no offload/segmentation, correct given every optional feature was declined) plus a minimal broadcast Ethernet frame, places a real descriptor in the TX queue's available ring, notifies the device (`QUEUE_NOTIFY`), and polls the used ring for completion - the VirtIO equivalent of `net::e1000::send_test_frame`'s own `TDH`-advancing proof, though a genuinely different protocol shape (a completion-ring index, not a single hardware register).** **Hit, diagnosed, and fixed the exact same class of bug `net::e1000` needed until its own Fase 44 to resolve - but this time in one re-test, not a multi-Fase investigation**: the very first attempt completed the entire register-level handshake correctly (`pfn_ok=true`, `final_status=0x07`) yet the used ring never advanced (`advanced=false`) - and the leading hypothesis, checked FIRST specifically because this kernel's own prior history already named it exactly, was that `net::virtio` had never called `PciDevice::enable_bus_mastering()` at all. Confirmed correct: a freshly enumerated PCI device starts with DMA (bus mastering) disabled by design, and without it a device can complete every *register* handshake step while still being unable to actually *read* the virtqueue memory itself via DMA - precisely the same real, standard PCI requirement `net::e1000` discovered the hard way. Adding the one missing call fixed it immediately: `used_idx_after=1` (up from `0`), `elem_id=0` (matching the descriptor used), `elem_len=0` (correct and expected for a TX completion - the device never writes back into a read-only buffer it only ever reads from, unlike an RX buffer) - identical and fully repeatable across 3 boots, zero regression across the entire prior self-test suite. This is the first genuinely complete, real TX round trip through this kernel's second NIC. **`net::virtio::init_rx_queue` (Fase 65) sets up the RX virtqueue (index 0 - confirmed, not assumed, against the Linux `virtio_net` driver's own `rxq2vq(0)=0`/`txq2vq(0)=1`) and arms one real, write-only receive descriptor - the RX equivalent of `init_tx_queue`'s own scope (Fase 63), taking the same two-step shape TX itself did (ring setup here, an actual received-frame proof as separate follow-on work).** Deliberately called AFTER the TX handshake had already signaled `DRIVER_OK`, without re-touching `STATUS` at all - real drivers are recommended to size every virtqueue before that point, but nothing here assumed QEMU's legacy `virtio-pci` actually enforces that ordering: `pfn_ok=true` is genuine, empirical proof this second queue's memory was still accepted by the device regardless. The one genuinely new piece beyond every descriptor this kernel has written so far: the flags word is `VRING_DESC_F_WRITE` (`2`, confirmed against the Linux kernel's own real `virtio_ring.h`) instead of TX's `0` - telling the device this buffer is for it to write INTO, the opposite direction from TX's read-only descriptors - and `desc_write_flag_ok=true` confirms that flag reads back correctly from the virtqueue's own memory, not just that the write didn't crash. Worked correctly on the very first attempt (`queue_num=256`, `frames_needed=3`, `pfn_ok=true`, `desc_write_flag_ok=true`, `avail_idx_after=1`), identical across 3 repeated boots, zero regression across the entire prior self-test suite (including TX's own `txq_test`/`tx_test` lines, unaffected by RX's setup running afterward on the same device). Does not yet trigger or observe a real received frame - proving that (the way `net::e1000`'s own Fase 45 did for its RX side) remains real, substantial follow-on work. **`net::virtio::receive_test_frame` (Fase 66) attempted that full round-trip proof - a real ARP request (the exact byte layout `net::e1000::send_test_frame` already proved SLIRP answers) sent through a fresh TX descriptor, then polling the RX buffer armed above for a genuine reply.** The TX half reliably works (`tx_phase=ok`, identical across repeated boots - a second real frame send through the same queue via a distinct descriptor, index 1, alongside `send_test_frame`'s own index 0). **The RX half was originally reported as honestly unresolved - Fase 78 finally cracked most of it.** The armed buffer stayed entirely untouched (all zeros) and its used index never advanced, even though `net::e1000`'s own later tests in the exact same boot independently confirmed SLIRP was still answering ARP/ICMP correctly. **The real cause, found after two prior "fresh eyes" rounds (this session's own Fase 66 investigation, then a later revisit) came down while comparing raw buffer bytes across two slightly different boot-command captures**: an explicit, independent `-netdev` per NIC showed the mystery MAC address `52:54:00:12:34:57` (virtio's own) sitting exactly where an incoming frame's destination-MAC field belongs, at offset 10 into the buffer - real evidence a frame HAD been DMA'd in, even though `used_idx` still read as stuck. Adding `used_idx_before`/`avail_idx_now` to the existing timeout diagnostic confirmed it directly: `used_idx_before` was already `1` (not `0`) the instant this function's own wait began. **Root cause**: `init_rx_queue` (Fase 65) arms exactly ONE descriptor, once, at boot - and `send_test_frame` (Fase 64, called earlier in the same boot) already sent its own ARP request, whose real SLIRP reply generally consumes that one-and-only descriptor before `receive_test_frame` ever gets a chance to look. The RX mechanism itself (DMA write, used-ring update, ISR) was working correctly the entire time, just for a reception this function structurally could never observe - not a device or backend bug at all. **Fixed** by re-arming a fresh descriptor into the avail ring immediately before this function's own wait - with one self-caught wrinkle along the way: the re-arm has to happen *before* this function's own ARP request goes out over TX, not after, or a fast-arriving reply can land with no descriptor available yet and get silently dropped, reproducing the identical symptom this fix was meant to eliminate (`net::e1000::arp_resolve`'s own module doc already states this exact ordering requirement - "arm the RX ring first" - which an earlier version of this same fix violated). **A second, narrower mystery remains open even after that fix**: with a freshly re-armed descriptor genuinely in place beforehand, this function's own ARP request - confirmed byte-perfect via a temporary diagnostic dump (correct broadcast destination, source MAC, EtherType, HTYPE/PTYPE/HLEN/PLEN/OPER/SHA/SPA) and confirmed TX-completed by the device (`used_elem_id` matching the right descriptor) - still never observably receives a reply, not within the normal ~5-second wait nor an extended ~22-second one tried specifically to rule out "just needs more time" (identical result either way). Querying a different target IP than `send_test_frame`'s own made no difference either (ruling out a same-target SLIRP-side suppression theory). So: two independent ARP round trips through the same queue in one boot - the first demonstrably completes, the second does not, with TX confirmed fine on both - a question specific to QEMU/virtio-net's own handling of a second reception on an already-once-used queue, not this driver's own request construction or ring bookkeeping. **Separately and orthogonally**, this exact investigation also confirmed the kernel's own standard boot command (`-device virtio-net-pci`, no explicit `-netdev` - what CI and `boot_kernel.bat` both actually run) gives the device NO working network backend at all: `raw_buf_head` stays all-zero and `used_idx` never advances even once, the entire boot, under that command specifically - contrasted directly against the explicit-independent-netdev capture where the first round trip above genuinely succeeds. QEMU's implicit default NIC (e1000, added because no `-net`/`-netdev`/`-nic` flag exists at all) appears to claim the one implicit backend QEMU creates in that case, leaving the explicitly-added `-device virtio-net-pci` with nothing attached - meaning CI will keep showing a timeout for this specific test regardless of the fix above, but for this separate, now-understood reason, not because the fix doesn't work. Deliberately left unchanged in this Fase: giving `virtio-net-pci` its own explicit `-netdev` would fix that half but not the second-round-trip mystery, so it wouldn't flip this specific test to a pass either way, and the shared boot command underpins every other CI check too - a bigger blast radius than this Fase's own scope justifies without that payoff. Both remaining questions were genuinely comparable to `net::e1000`'s own DD-bit mystery, which took two separate investigation rounds (Fase 22, Fase 32) before its real resolution (Fase 44) turned up while researching something else entirely. **Fase 82 finally resolved BOTH, and the resolution came from looking outside this kernel's own source entirely.** Three further investigation rounds (this project's own memory calls them rounds 3-5) fetched and read the REAL, exact-pinned-revision source for QEMU's own network glue (`net/slirp.c`) and libslirp itself (`arp_input` in `slirp.c`, hosted on GitLab, its exact revision found via QEMU's own `subprojects/slirp.wrap`) - both confirmed completely stateless with respect to request history, ruling out any SLIRP/QEMU-side reply-suppression theory as a class, not just individual guesses about it. The actual breakthrough came from a genuinely new kind of evidence: a real packet capture (QEMU's own `-object filter-dump` attached directly to the virtio-net netdev) proved SLIRP replies in under 100 microseconds - the reply was never missing at all. A QEMU-side virtqueue trace (`-trace events=virtqueue_fill,virtqueue_flush,...`) then showed the device's own `virtqueue_flush` for the RX queue genuinely firing, for real, exactly once. Lining that up against this function's own source order revealed the actual bug, and it was entirely this kernel's own: `used_idx_before` for the RX side was sampled AFTER this function's own TX notify (and after that TX completion was even polled and confirmed), not before. SLIRP is an in-process, fully synchronous backend with no real async socket I/O needed for ARP - the entire request -> SLIRP -> reply -> RX-delivery chain can complete inside the single `out` instruction that notifies the TX queue, before that instruction even returns. By the time the old code sampled its baseline, the one-and-only completion had already happened - the wait loop was watching for a change that had already occurred, and could only ever time out no matter how long it waited. **Fixed** by moving that baseline sample to immediately after the RX re-arm, before this function builds or sends its own request at all. Verified: `is_arp_reply=true`, `gateway_ip=Some([10, 0, 2, 2])` - the actual success path, reached for the first time in this mystery's entire history, identical across 3 repeated boots, full regression check clean against the complete pre-existing assertion suite. Closing this out also finally gave the still-open "no working backend under the standard boot command" issue a real payoff: the shared boot command (`kernel-ci.yml` and `boot_kernel.bat` both) now gives e1000 and `virtio-net-pci` their own explicit, independent `-netdev user` each, verified beforehand (3 repeated boots under this exact dual-netdev config, the complete 110-assertion regression suite unchanged, e1000's own PCI slot/MAC/every existing e1000 test untouched) to justify finally touching a boot command every other CI check also depends on. **Fase 83 takes the lower-risk stepping stone this kernel's own ring-0 scheduler history already validated: cooperative multi-tasking before preemptive.** Genuine mid-flight ring-3 preemption - a timer tick interrupting a running ring-3 program at an arbitrary point it didn't choose - needs the timer handler itself to become a full-GPR-saving naked stub, a substantially bigger and riskier undertaking than anything built so far in this arc. Cooperative multi-tasking sidesteps that entirely: two ring-3 tasks voluntarily hand off to each other at a POINT THEY THEMSELVES CHOOSE, via a new, fourth dedicated DPL=3 vector (`0x83`), with zero changes to the timer interrupt path at all. The mechanism mirrors `switch_to` almost exactly: the yield handler pushes the SAME 6 callee-saved registers `switch_to` already preserves, plus a manufactured "return-to-trampoline" address (the identical trick Fase 81's own `prepare_ring3_initial_stack` already established) - meaning a YIELDED task's own saved state ends up in the exact same shape a FRESH bootstrap already has, so one resume path handles both uniformly. Both ring-3 "tasks" in the test share the same instruction bytes (mirroring how `scheduler::preemptive`'s own two ring-0 tasks already share one function body, parameterized by an id) - each has a tiny, distinct prologue loading its own task id and a shared signature-buffer address, before jumping into shared logic. Real, ordered proof of correct interleaving: task 0 writes `'A'`, yields - resuming task 1 for the very first time via the yield path itself (not a direct `switch_to` call from Rust, proving the yield mechanism can originate a task's first run, not just hand off between two already-started ones); task 1 writes `'B'`, yields back; task 0 resumes and writes `'C'` - real proof its own task-id register survived the full round trip - then exits via the existing Fase 81 vector. **A real bug, caught by this test's own very first boot, not glossed over**: the first version saved a POINTER into RSP0 (the CPU's shared, privilege-elevation-entry stack) instead of copying the snapshot elsewhere - broken because the CPU reloads RSP0 fresh from the TSS on EVERY subsequent ring3-to-ring0 transition, so task 1's own later yield silently overwrote task 0's still-referenced snapshot before it was ever read back. Task 0 came back reporting task 1's own register state instead of its own (`signature=[0x41,0x42,0x44]` instead of the expected `[0x41,0x42,0x43]`) - an exact, diagnosable symptom that pinpointed the bug immediately. Fixed by copying the 96-byte snapshot into each task's own dedicated, never-shared kernel memory instead of just remembering a pointer into transient, reused space. Verified: `signature=[0x41, 0x42, 0x43]` ("ABC") - exact, ordered, ONLY EVER POSSIBLE if the interleaving genuinely happened in the correct sequence with the correct state - identical across 3 repeated boots (2 independently-fresh-disk boots plus one on an already-grown local disk, all agreeing), full regression check clean against the complete pre-existing 111-assertion suite. Task 1 is deliberately never resumed again after its own single yield (a real N-task, fully general ring-3 scheduler - more than 2 tasks, priority, tasks exiting independently - is real, separate, more substantial follow-on work). **Fase 84 takes the first real step toward that: the timer IRQ's own entry point converted from a compiler-generated `extern "x86-interrupt"` fn to a hand-written naked stub that explicitly saves ALL 15 general-purpose registers**, the same shape, push/pop order, and CS.RPL-read-at-a-fixed-offset trick `syscall_entry_asm` (Fase 72/75) already established and this kernel already relies on working, reused here rather than re-derived. Deliberately a PURE, behavior-preserving refactor - this Fase adds no new capability and changes no CI assertion at all. The reason it matters: `extern "x86-interrupt"` only ever transparently preserves whatever THIS SAME invocation's own compiled body happens to touch, on the assumption its own epilogue will eventually restore them for the identical interrupted context - it has no way to support "capture the FULL register state now, resume a DIFFERENT context later," which is exactly what genuine ring-3 preemption needs (unlike Fase 83's own cooperative yield, where the ring-3 program itself chooses the moment and only needs the same minimal 6-register convention `switch_to` already uses). This Fase's own `handle_timer_tick` function is byte-for-byte the same logic the old handler already had - the only thing that changes is how many registers are actually captured at the moment a tick lands, not what happens with them yet. Verified via the complete pre-existing 112-assertion CI suite passing unchanged (there is nothing new to observe yet) - given the criticality of the timer path, verified with extra care: 3 repeated boots, zero faults, every existing timer-dependent test (the preemptive scheduler demo, both ring-3-concurrent-with-preemption tests, the cooperative ring-3 test) producing byte-identical results, full-diff regression clean against only the usual known-benign RTC/tick-count/local-disk-persistence variance. Genuine mid-flight, timer-driven ring-3 preemption - actually USING these now-fully-captured registers to switch a running ring-3 program out for something else - remains the single largest piece of real, substantial follow-on work in this kernel. **Fase 85 takes that first real step: genuine, INVOLUNTARY (timer-driven) ring-3 preemption, not just the naked-stub plumbing Fase 84 prepared.** A new `scheduler::ring3_preempt` module threads a `saved_ctx_ptr` (the base of the 15-GPR block Fase 84's stub now saves) through `handle_timer_tick`, which returns a `u64` RSP for the naked stub to resume from via `mov rsp, rax` - the same "hand off to a possibly-different context" shape `switch_to` and Fase 83's own yield handler already established, but reached from an involuntary timer tick instead of a voluntary vector this time. The self-test is deliberately the SIMPLEST possible proof, not full task-switching yet: a ring-3 program loads 5 registers Fase 83's own 6-register convention never protects (`eax`/`ecx`/`edx`/`esi`/`edi` - genuinely different from `switch_to`'s callee-saved set) with distinct patterns, spins Fase 79's own proven `LOOP_COUNT=150_000_000` (long enough for a real tick to land), then XORs all 5 into `eax`. `scheduler::ring3_preempt::tick` copies the 160-byte snapshot (15 GPRs + the CPU's own 5-field iretq frame) into dedicated memory and - deliberately reusing the exact bug class Fase 83's own RSP0 mistake taught this codebase - POISONS the original location (`0xAA` fill) before resuming, so a correct result is only possible if the resume genuinely reads from the copy, not a stale live pointer. `exit_code=0x11111111 intercepted=true`: the checksum only comes out right if all 5 registers survived a real, involuntary interruption at an unpredictable point correctly, and `intercepted=true` confirms the new mechanism genuinely claimed a real tick rather than the loop just finishing first. Passed on the very first boot, identical across 5 repeated runs (5, not the usual 3, given how novel and timing-sensitive a first-ever async save/resume is) - zero regression across the complete existing suite. Deliberately does NOT yet run a different task while the original is preempted (the save/resume round trip returns to the SAME interrupted program) - genuine multi-task ring-3 scheduling via the timer, where a preempted task actually yields its CPU slot to a DIFFERENT ring-3 program, remains the real, larger follow-on work this Fase was designed to de-risk. **Fase 86 takes that real, larger step: a new `scheduler::ring3_mt` module genuinely switches between TWO DIFFERENT ring-3 programs on every real timer tick, not just saving-then-resuming the same one.** Mirrors `scheduler::preemptive`'s own ring-0 round-robin shape (two tasks, alternating unconditionally - deliberately hardcoded, the same simplification Fase 83's own cooperative test already chose), but switching FULL 160-byte (15-GPR + 5-field `iretq` frame) contexts instead of ring-0's much smaller 6-register `switch_to` convention, for the same reason Fase 85 itself needed the bigger shape. Task 0 is entered completely normally (`enter_ring3`/`int 0x81` exit, EXACTLY Fase 85's own test program reused verbatim except its 5 register constants use a `0x1...` pattern); task 1 is entered ONLY by being switched into cold, via a NEW `prepare_ring3_mt_task_ctx` bootstrap builder that lays out a fresh 160-byte context directly (15 zeroed GPRs + a real `iretq` frame) - genuinely SIMPLER than Fase 81's own `switch_to`-oriented bootstrap, since resuming via the timer stub's own `iretq`-based tail needs no manufactured "return-to-trampoline" indirection at all. Task 1 never voluntarily exits (two tasks racing for the single existing `int 0x81` slot would break the "kernel regains control exactly once" invariant every other ring3 test relies on) - instead it finishes its own smaller loop, computes its own checksum (a `0x2...` pattern), and spends the rest of the experiment in a harmless, register-preserving infinite spin; verification reads task 1's own LAST-saved `eax` directly out of its dedicated context buffer, valid specifically because the spin never touches it. **A real bug, caught by this mechanism's own very first boot**: `run_multitasking`'s initial version left task 0's own context slot as a bare `0` (reasoning, WRONGLY, that since it's only ever written before being read, it didn't need a real destination buffer) - the very first real preemption tried to copy 160 bytes into address `0x0`, an immediate page fault. Fixed by allocating a real, dedicated buffer for BOTH tasks' slots up front, not just task 1's. Verified after the fix: `task0_checksum=0x10000004 task1_last_eax=0x20000004 switch_count=12` - both checksums exactly the expected, independently-computable constants, and 12 genuine involuntary switches back and forth (not just one lucky round trip) - identical across 5 repeated boots, full regression check clean against the complete pre-existing 116-assertion suite. Deliberately still not a general N-task, priority-aware scheduler (exactly 2 hardcoded tasks, one one-directional completion signal) - that remains real, separate, further follow-on work, but the core question this whole arc has been building toward since Fase 79 - can this kernel genuinely run more than one ring-3 program by taking turns via real, involuntary hardware interrupts - is now answered yes. **Fase 91 takes the "deliberately still not a general N-task scheduler" gap this Fase's own paragraph flagged and generalizes it from exactly 2 hardcoded tasks to a real 3-task round-robin** - not an arbitrary bigger number, but the smallest scale that actually proves the mechanism isn't secretly still a hardcoded pair, the same "one more, not an arbitrary many" reasoning this session's own GGUF/quantization Fases already applied one variant at a time. `scheduler::ring3_mt`'s own `tick()` function turned out to already be written generically (`(current + 1) % active_count`, unchanged since Fase 86) - the real work was replacing the fixed 2-slot context array with a `RING3_MT_MAX_TASKS=4` headroom array plus a runtime `RING3_MT_ACTIVE_TASKS` count, and generalizing `run_multitasking` to take a slice of "other task" contexts (task 0 alone is still entered normally, exactly as before) instead of exactly one, returning each one's own last-observed checksum as a list instead of a single value. `ring3.rs` gained a third self-checking ring-3 program (register pattern prefixed `0x3...`, its own small loop count, its own entry/stack addresses within the same single mapped test page) alongside the existing two, with the shared "load 5 registers, spin, XOR them into `eax`, exit-or-spin" shape factored into one helper instead of hand-copied a third time. Verified: all 3 tasks' checksums (`task0=0x10000004`, `task1=0x20000004`, `task2=0x30000004`) came out exactly right on the very first attempt, identical across 5 repeated boots (switch counts varying run to run - 15 to 21 - itself real evidence of genuine, timer-driven, non-deterministic interleaving rather than a fixed script), zero regression across the complete pre-existing self-test suite. Deliberately still does not add priorities to this scheduler (every task is treated identically - plain round-robin, no tiers) or let any task besides task 0 signal its own completion - both remain real, separate, further follow-on work, the same relationship Fase 86-then-87 already had for the ring-0 preemptive scheduler. **Fase 92 closes that gap**, adding real priority to `scheduler::ring3_mt` by porting `scheduler::preemptive::tick`'s own algorithm (Fase 87) almost unchanged: find the highest actual priority among the active tasks, round-robin only within that top tier, starting right after whichever task last ran. A real design constraint shaped the self-test here that the ring-0 case never had to worry about: `run_multitasking` has no independent tick budget of its own - it only ever returns once task 0 itself reaches `int 0x81` - so a test that let some OTHER task out-prioritize task 0 forever would hang the entire boot with no safety net at all. The test therefore gives task 0 the TOP priority (`Priority::High`, guaranteed to keep winning and complete normally) and proves starvation from the other side instead: tasks 1 and 2 (`Priority::Background`) get real, safe, distinguishable programs written to memory (in case a real bug ever let them run) but are expected to NEVER execute even once while task 0 is ready - checkable because their own cold-bootstrap context starts with a zeroed `eax`, untouched unless the scheduler genuinely selects them. Verified: `task0_checksum=0x10000004` (task 0 completed normally) with `task1_last_eax=0x0 task2_last_eax=0x0` (neither background task ever ran, not even once) and a positive `switch_count` (5-6 across 5 repeated boots - real ticks kept landing and being processed the whole time, they just never once caused a switch away from task 0) - on the very first attempt, identical across all 5 boots, zero regression, no hang. **Fase 93 closes another gap both Fase 86 and 91 explicitly left open: a NON-task-0 `ring3_mt` task can now voluntarily retire early - stop being scheduled for good, mid-session - instead of spinning forever being the only alternative to task 0's own `int 0x81` exit.** A new, FIFTH dedicated DPL=3 vector (`0x84`) reuses the timer stub's own exact full-15-GPR asm shape verbatim (the same 160-byte context format `scheduler::ring3_mt` already uses), reached voluntarily via `int 0x84` instead of by hardware - its Rust-side handler, `task_done`, saves the retiring task's context (identically to `tick`), marks it in a new `RING3_MT_TASK_DONE` bitmask, and picks the next task via a selection helper now SHARED between `tick` and `task_done` (factored out specifically so both skip done slots the same way they already skip lower-priority ones). Task 0 itself never uses this new path - it still exits exclusively through the original, unmodified `RING3_EXIT_INT_VECTOR` mechanism, which is what guarantees the selection search can never come up empty even if every OTHER task has retired. The self-test isolates this one new variable deliberately: all 3 tasks share `Priority::Normal` (Fase 91's own round-robin test, unchanged) - task 1 now ends via the new `ProgramEnding::RetireViaInt84` instead of spinning forever, while task 2 keeps the OLD `ProgramEnding::SpinForever` unchanged as a direct control. Verification is stronger than a checksum alone: `run_multitasking`'s own new `other_done` return value reports each task's retirement status straight out of the done-mask itself - `task1_done=true` is real, independent proof the new vector fired and was recognized (not merely inferred from a correct checksum, which a bug leaving task 1 merely un-resumed after one last correct tick could also produce), while `task2_done=false` proves the unrelated control task's own old behavior is completely untouched. Verified: `task0_checksum=0x10000004 task1_last_eax=0x20000004 task1_done=true task2_last_eax=0x30000004 task2_done=false` with a genuinely varying `switch_count` (11-13 across 5 repeated boots) - correct on the very first attempt, identical across all 5 boots, zero regression, no hang, no new bug found this Fase. **Fase 94 turns to the OTHER ring-3 scheduling mechanism this arc has left un-generalized since Fase 83: the cooperative-yield path (`ring3_coop_yield_helper`) is now a real N-task (3) round-robin, not exactly 2 hardcoded tasks alternating unconditionally** - the same generalization Fase 91 already did for the involuntary-timer path (`scheduler::ring3_mt`), applied here for the first time. `RING3_COOP_TASK_RSP` grew from a fixed 2-slot array to a `RING3_COOP_MAX_TASKS=4`-slot one with a runtime `RING3_COOP_ACTIVE_TASKS` count, and the yield helper's old hardcoded `1 - cur` toggle became real round-robin arithmetic, `(cur + 1) % active_tasks`. The self-test's own 3 tiny ring-3 "programs" needed a genuinely new technique to build safely: each 16-byte prologue's trailing `jmp` now has its relative offset COMPUTED by a new `build_coop_task_prologue` helper (from the prologue's own position and the shared body's position) rather than 3 separately hand-derived hex constants - deliberately safer given this exact class of hand-encoded x86 already caused a real bug once before, in Fase 83 itself. Real, ordered proof of 3-way interleaving, not just "nothing crashed": task 0 writes `'A'`, yields to task 1; task 1 writes `'B'`, yields to task 2 (**not back to task 0** - the genuinely new proof that round-robin visits every task in turn rather than merely ping-ponging between the first two); task 2 writes `'C'`, yields, wrapping back around to task 0, which resumes exactly where it left off and writes a deliberately distinct completion marker (`0x58`, chosen so it can never collide with `'A'`/`'B'`/`'C'`) before exiting normally. Verified: `exit_code=42 signature=[41, 42, 43, 58]` - exact on the very first attempt (the computed-offset technique introduced no encoding bugs), identical across all 5 repeated boots, zero regression against the complete pre-existing suite, no new bug found this Fase either. What remains open after Fase 94: neither ring-3 scheduling mechanism (cooperative-yield or involuntary-timer) knows about the OTHER's own generalizations except where explicitly ported one at a time as in this Fase - a real, shared abstraction unifying them remains unstarted, real, separate follow-on work, as does priority-awareness and early-exit for this specific (cooperative-yield) mechanism, mirroring what Fase 92/93 already added to the timer-driven one. **Fase 95 closes half of that gap: `ring3_coop_yield_helper` now has real priority too**, porting the exact same tier-search-then-round-robin algorithm `scheduler::ring3_mt::tick` already uses (Fase 92) - find the highest actual priority among active slots, round-robin only within that top tier. **The safety argument is genuinely different here, because this mechanism is VOLUNTARY, not timer-driven**: `ring3_mt`'s own hang risk was "some OTHER task could out-prioritize task 0 forever" (nothing else ever transfers control except a real hardware tick). Here, nothing transfers control except a task's OWN explicit `int 0x83` - so if task 0 alone holds the top priority tier and it voluntarily yields, the tier search finds no OTHER slot at that tier and round-robins all the way back around to task 0 ITSELF, a safe immediate self-resume rather than a hang. `run_priority_cooperative_test` proves exactly this: task 0 (`High`) writes `'A'`, yields, immediately resumes itself, writes its own completion marker, and exits normally - while tasks 1 and 2 (`Background`) never run even once, proven the same negative way Fase 92 already established (their own signature slots, where they'd write `'B'`/`'C'`, stay at the zero-fill nothing else ever touches). Deliberately reuses the EXACT SAME program `build_cooperative_test_program` already builds for the plain round-robin test (Fase 94) - priority is purely a scheduling-setup concern, not a program-construction one, so no new hand-encoded x86 was needed at all this Fase. Verified: `signature=[41, 00, 00, 58]` exact on the first attempt, the pre-existing round-robin test's own `[41, 42, 43, 58]` result completely unchanged (confirming equal-priority reduces to plain round-robin exactly as intended), identical across all 5 repeated boots, zero regression, no new bug found this Fase. What remains open after Fase 95: (a) the cooperative-yield mechanism still has no early-exit-before-the-caller-decides (mirroring what Fase 93 added to the OTHER mechanism) - the other half of the gap Fase 94 flagged. (b) the two ring-3 scheduling mechanisms still share no real shared abstraction, only mirrored algorithms ported by hand one at a time. **Fase 96 closes the other half: a NON-task-0 cooperative-yield task can now voluntarily retire for good too**, completing the mirror of all 3 generalizations (N-task, priority, early-exit) onto BOTH ring-3 scheduling mechanisms this arc has built. A new, SIXTH dedicated DPL=3 vector (`0x85`) reuses the yield vector's own exact 96-byte-context naked-asm shape (NOT `scheduler::ring3_mt`'s own 160-byte one, since this mechanism uses `switch_to`'s smaller convention throughout) - its handler, `ring3_coop_task_done_helper`, saves the retiring task's context, marks it in a new `RING3_COOP_TASK_DONE` bitmask, and picks the next task via a `coop_select_next` helper now SHARED with the plain yield path (mirroring the identical `select_next` factoring Fase 93 already did for the OTHER mechanism). Task 0 never uses this new path - it still exits exclusively through the unrelated `RING3_TASK_EXIT_INT_VECTOR`, guaranteeing the tier search can never come up empty. Task 1 gets a genuinely different, SHORTER "retire body" (write `'B'`, then `int 0x85` instead of `int 0x83` - no "second half" to reach, since retiring means it never runs again) while tasks 0/2 keep the unchanged yield body. Real, ordered proof: task 0 writes `'A'`, yields to task 1; task 1 writes `'B'`, retires (rather than yielding) - `coop_select_next` correctly picks task 2 NEXT (not task 1 again); task 2 writes `'C'`, yields, correctly wrapping around to task 0 (skipping the now-retired task 1), which resumes and exits normally. Verification is stronger than the signature alone (which numerically matches the plain round-robin result, `[41, 42, 43, 58]` - task 1 reaches `'B'` either way): reading `RING3_COOP_TASK_DONE` directly gives independent, structural proof - `task1_done=true` while `task0_done=false`/`task2_done=false`. Verified: exact on the very first attempt, identical across all 5 repeated boots, zero regression against the complete pre-existing suite (both pre-existing cooperative tests staying byte-identical), no new bug found this Fase either - the cooperative-yield generalization work across Fase 94/95/96 has been remarkably clean throughout, likely because the underlying save/restore and tier-search primitives were already thoroughly proven before each new generalization. What remains open after Fase 96: the two ring-3 scheduling mechanisms still share zero code - only mirrored algorithms, hand-ported one at a time across 6 Fases (83→94 for N-task, 87→92→95 for priority, 81→93→96 for early-exit lineage) - a real, shared abstraction unifying them into one mechanism remains unstarted, substantial, separate follow-on work, as does human review of the underlying hand-written context-switch asm. **Fase 97 takes a first, deliberately small and safe slice of that unification: the pure-Rust tier-search-then-round-robin algorithm itself - `ring3_mt::select_next` and `ring3_coop`'s own `coop_select_next` - turned out to be byte-for-byte identical the whole time, differing only in which same-valued-but-differently-named `MAX_TASKS` constant each closed over.** A new `scheduler::tier_select` module holds ONE const-generic `select_next<const N: usize>` both mechanisms now call directly; the two old local copies are deleted. Deliberately does NOT touch the two mechanisms' genuinely different context-save shapes (160-byte full-GPR vs 96-byte callee-saved), asm entry stubs, or per-mechanism static state - those stay exactly as separate as they've always needed to be. A pure, behavior-preserving refactor: no new CI assertion, since there is nothing new to observe - the complete pre-existing 126-assertion suite passing byte-identical (all 6 ring-3-scheduling self-tests' own signatures/checksums/done-masks unchanged) across 5 repeated boots IS this Fase's whole proof. What remains open after Fase 97: the two mechanisms' own context formats, asm stubs, and static state are still fully separate - a real, complete merge into one mechanism remains substantial, unstarted follow-on work, this Fase only ever having unified the one piece that was genuinely safe to unify without touching either mechanism's own proven-correct asm. **Fase 98 closes a genuinely different, longstanding gap: every ring-3 program this entire arc has ever run (Fase 71 through 97) has been a `[u8; N]` byte array compiled directly into this kernel's own source - never bytes that came from outside it.** `ring3::run_ring3_disk_loaded_test` writes the exact same 15-byte program `run_ring3_exit_test` (Fase 73) already uses to a real file via `fat12::create_file`, reads it back via `fat12::read_file` - the same write/read/delete round trip the GGUF disk-load tests (Fase 41) already established - and copies THOSE returned bytes, not the original constant, into the user page before calling `enter_ring3`. Deliberately does not build any kind of general program loader (parsing an executable format, placing multiple segments, allocating a fresh page per program); it only proves the one new fact everything else would depend on. Spawns no PCB entry, so it runs safely before the PID-sensitive priority-preemptive test that must stay last. Verified: `roundtrip_ok=true exit_code=42` - the disk round trip was byte-for-byte exact, and the disk-loaded bytes are genuinely valid, executable ring-3 code producing the exact same result direct embedding already does - correct on the very first attempt, identical across all 5 repeated boots, zero regression against the complete pre-existing 126-assertion suite (only the usual local-only VirtIO/TCP and disk-persistence artifacts, neither new). What remains open after Fase 98: a real loader for arbitrary, larger programs (multiple pages, a real executable format, per-process page tables instead of one shared test page) remains substantial, separate follow-on work - this Fase only proves the disk-to-ring-3 path itself is sound. **Fase 99 closes one piece of that still-open gap: the disk-loaded program now runs on its own independent page (`USER_DISK_PROGRAM_PAGE_ADDR`) instead of sharing `USER_TEST_PAGE_ADDR` with every other ring-3 test.** `memory::user_page::map_user_page_at`/`inspect_user_page_at` generalize Fase 70's original single-page mapping/inspection functions (which become thin wrappers, so every existing test stays untouched); the new address's PML4 index (204) was checked against both the heap's (136) and the original test page's (170) before writing any code, so the mapping creates entirely fresh intermediate page tables rather than risking silently inheriting either existing mapping's flags. Verified the same two independent ways as Fase 70's own page: `disk_program_page present=true writable=true user_accessible=true write_read_back_ok=true`. The disk-loaded program still produces `exit_code=42` from this entirely new address - real proof that entering ring-3 was never implicitly hardwired to the one historical shared page. Correct on the first attempt, identical across all 5 repeated boots, zero regression against the complete pre-existing 127-assertion suite (only the usual local-only VirtIO/TCP and disk-persistence artifacts, neither new). Does not attempt the rest of the general-loader gap (multiple segments, a real executable format, per-process page tables rather than one more individually-mapped page) - that remains separate, substantial follow-on work. **Fase 100 closes another named sub-item: the disk-loaded program now has its own dedicated stack page (`USER_DISK_PROGRAM_STACK_ADDR`) instead of sharing the tail of its own code page.** Every ring-3 test in this kernel's history before this one set `stack_top` to `code_addr + 4096` - the same page as the code, just a different offset - so the `iretq`-based ring-3 transition had never actually been proven to work when code and stack live on genuinely separate, non-adjacent pages. The new address's PML4 index (238) was checked against all three prior mappings (the heap's 136, the test page's 170, the disk program page's 204) before writing any code. `enter_ring3` needed no changes at all: `user_rsp` was already an independent parameter, never assumed adjacent to `entry`. Verified: `disk_program_stack present=true writable=true user_accessible=true write_read_back_ok=true`, and the disk-loaded program still produces `exit_code=42` - real proof the transition holds even with code and stack on entirely separate pages. Correct on the first attempt, identical across all 5 repeated boots, zero regression against the complete pre-existing 128-assertion suite. The remaining general-loader gap (multiple segments, a real executable format, per-process page tables) is still separate, substantial follow-on work. **Fase 101 returns to the OTHER open thread instead - the ring-3-mechanism unification Fase 97 started.** Found (by direct inspection, not assumption) that `ring3_coop`'s own 3 self-tests each inlined a byte-for-byte-identical "arm the tasks" block (assign each task's RSP/priority, reset the done-mask, set active-count/current-index) - unlike `scheduler::ring3_mt`'s own `run_multitasking`, which every `ring3_mt` self-test already calls through ONE shared entry point, `ring3_coop` had no equivalent before this. Extracted `arm_ring3_coop_tasks(tasks: &[(u64, u8)])`, generalized over a slice rather than hardcoded to 3 (mirroring `run_multitasking`'s own N-task design), and repointed all 3 tests at it. Deliberately does not touch `prepare_ring3_initial_stack` or any of the 3 tests' own different post-run read-back/cleanup logic, and does not touch either mechanism's differing context format or asm - purely the shared setup slice, the same scope discipline Fase 97 itself used. Pure refactor, no new CI assertion - proof is all 3 coop self-tests' own signatures (`[41,42,43,58]`, `[41,00,00,58]`, and the early-exit done-mask) staying byte-identical across 5 boots. **Fase 103 returns to the disk-loaded-program thread (Fase 98-100) and closes another piece of the general-loader gap: the program now writes to a real DATA segment, not just code and stack.** Adds a FOURTH independent page, `USER_DISK_PROGRAM_DATA_ADDR` (PML4 index 102, checked distinct from all three prior mappings before writing any code), reusing Fase 99's own `map_user_page_at`/`inspect_user_page_at` verbatim. The 15-byte program becomes a 31-byte one, built by a new `build_disk_loaded_program(data_addr)` that embeds the data page's own address directly into the machine code (`mov r12, imm64`) and writes a real signature byte through it (`mov al, 0x99` / `mov [r12], al`) - the exact encoding `build_coop_task_prologue`'s own multi-task self-tests already proved correct for feeding a hand-assembled ring-3 program a runtime address, reused rather than inventing a new one. Ring-0 reads the data page back after the program exits (legible regardless of `USER_ACCESSIBLE`, which only ever restricts ring-3) to independently confirm the write actually landed. Verified: `roundtrip_ok=true exit_code=42 data_write_verified=true` - correct on the first attempt, identical across all 5 repeated boots, zero regression against the complete pre-existing 130-assertion suite. The general-loader gap is now down to one item: an actual executable format, rather than a flat byte blob. **Fase 104 closes that last item, in its smallest well-scoped form: the disk file is no longer just raw program bytes - it's a real, minimal header the kernel actually parses.** A genuine 12-byte format (magic `b"AGP1"`, then `code_len`/`data_len` as little-endian `u32`s), deliberately not ELF or any existing real format (both substantially larger than this Fase needs) - `parse_program_header` validates the magic, then checks (via `checked_add`, so a header lying about huge lengths fails cleanly rather than overflowing into a too-small sum) that the buffer's REAL length exactly matches `header + code_len + data_len` before ever trusting those lengths to slice anything. The kernel writes header+code+a real 4-byte initial-data blob to disk, reads the whole buffer back (the same round-trip-honesty discipline as every Fase since 98, just on a bigger buffer), parses the header, copies the header-declared code region to the code page and the header-declared initial-data region to the data page - BEFORE the program ever runs, verifying that copy immediately and independently of the program's own later runtime write to the exact same page. Verified: `roundtrip_ok=true exit_code=42 data_load_verified=true data_write_verified=true` - two genuinely independent proofs (the kernel's own header-driven load, and the program's own runtime write, at two different times, to the same page) both landing correctly - correct on the first attempt, identical across all 5 repeated boots, zero regression against the complete pre-existing 131-assertion suite. The general-loader gap, as originally scoped (executable format, multiple segments, a program's own stack), is now fully closed - a genuinely variable-count, arbitrary-address segment table (rather than this Fase's own fixed code+data split) remains real, separate, substantially larger follow-on work if ever needed. **Fase 105** returns to the ring-3-mechanism-unification thread (open since Fase 97, untouched for 3 Fases) instead of continuing the loader thread a 5th time - closes a genuine capability gap rather than another cross-mechanism refactor like Fase 97/101: `scheduler::ring3_mt` (the timer-driven mechanism) could only ever give up a task's turn involuntarily (the timer) or permanently (`int 0x84`'s own retire, Fase 93) - it had no voluntary YIELD, the exact capability `ring3_coop` (the cooperative mechanism) has had since Fase 83 via `int 0x83`. A new seventh DPL=3 vector, `int 0x86`, closes that gap: its naked-asm entry stub is byte-for-byte the same 160-byte full-GPR shape `int 0x84`'s own stub already reuses from the timer handler (a voluntary yield must be resumable exactly like an involuntary preemption, since some other task could clobber any register), but its `call` target is `scheduler::ring3_mt::tick` - the UNCHANGED function a real timer tick already calls - instead of `task_done`, since yielding keeps the task eligible rather than retiring it. Zero changes needed to `ring3_mt.rs` itself. The new self-test's task 1 spins a small loop, writes `0x20000001`, yields via `int 0x86`, and - only if genuinely resumed - spins a second loop and writes `0x20000003` before retiring via `int 0x84`; task 2 keeps the unrelated `SpinForever` control unchanged. `task1_last_eax=0x20000003` (not `0x20000001`, the value it would be stuck at forever if the yield never resumed) is structural, not coincidental, proof the resume genuinely happened; `task1_done=true` additionally proves the pre-existing retire mechanism still composes correctly with a task that yielded first. Clean on the first attempt, identical across 5 boots, zero regression against the complete pre-existing 131-assertion suite (132nd pattern added). **Fase 106** leaves the ring-3 arc entirely for a fresh, previously-unstarted capability: the first real UDP round trip this kernel has ever attempted (every prior network Fase built TCP or ICMP). `guestfwd` was directly confirmed NOT to support UDP before anything was designed around it - tried locally, QEMU rejected `guestfwd=udp:...` outright as an "Invalid guest forwarding rule" - so the exact controlled, always-on peer every TCP test in this kernel depends on simply isn't available for UDP. Instead targets SLIRP's own built-in DNS proxy at `10.0.2.3:53`, always present for any `-netdev user` instance with zero extra QEMU flags. New `net::udp` module mirrors `net::tcp`'s own structure exactly (reuses `icmp::internet_checksum` for the RFC 768 pseudo-header sum) plus one genuine wrinkle TCP doesn't have: a computed checksum of exactly `0x0000` must be transmitted as `0xFFFF` instead, since IPv4 UDP reserves `0x0000` to mean "no checksum was computed". `net::e1000::dns_query_test` sends one fixed, hand-built 29-byte "A? example.com" DNS query (this is NOT a DNS resolver - the reply is only checked far enough to confirm it's genuinely a matching response: transaction ID and the QR bit, never any resource-record data) and mirrors `ping`'s exact TX/RX-ring structure. Caught and fixed a real bug before it ever ran: the first draft passed the RX buffer's untrimmed remainder straight into the checksum function, missing the exact Ethernet-padding pitfall Fase 89's own `tcp_syn_test` bug already taught this codebase - fixed by trimming to the IP header's own Total Length field first, the same discipline `tcp_syn_test` established. Genuinely uncertain going in whether SLIRP's DNS proxy would reply at all (unlike `ping`'s own gateway target, this depends on real network conditions) - only the deterministic `sent=true` is asserted in CI; `reply_received`/`qr_bit_set`/`answer_count` are logged honestly. Real local result: `reply_received=true qr_bit_set=true answer_count=0`, identical across 5 boots - a genuine bidirectional UDP round trip, though SLIRP's own proxy answered with zero records (not a bug in this kernel: `qr_bit_set=true` and the transaction-ID match alone already prove the round trip; this Fase deliberately never attempts real DNS resolution). Clean after one real bug caught before ever booting, zero regression against the complete pre-existing 132-assertion suite (133rd pattern added). **Fase 107** closes the exact gap Fase 106's own doc left open: confirming a UDP/DNS reply exists (transaction ID + QR bit) is not the same as extracting a real value from it. New `net::dns` module (application-layer, sitting one level above `net::udp` the same way `net::tcp` sits above `net::icmp`'s IPv4 work) parses the FIRST answer record of a DNS response and returns its IPv4 address when it's a plain A record - handling exactly one real-world shape (the answer's own NAME field as a 2-byte RFC 1035 compression pointer, what every real server actually sends) rather than general DNS message parsing. Verified with a realistic, hand-built fake reply (header + a 17-byte "example.com" question + one compressed-name A record) through 4 checks: the happy path, plus 3 single-byte mutations of the SAME fixture each proving the parser correctly rejects zero answer records, a non-A record type, and an uncompressed name - the same not-a-rubber-stamp discipline the ICMP/TCP self-tests already established with their own corrupted-checksum cases. This pure-logic self-test is the ONLY way to verify the parsing logic at all on this kernel's own local dev machine, where Fase 106 already observed zero real answer records ever arrive - wired live into `dns_query_test` too (attempts the real parse whenever `answer_count > 0`), logged honestly (`parsed_a_record`) rather than asserted on, since real-world DNS-proxy behavior is genuinely environment-dependent (Fase 106's own CI run saw `answer_count=2` where this local machine saw `0`). Clean on the first attempt (only a routine `clippy::type_complexity` catch on the extended return type, fixed with a type alias per clippy's own suggestion), zero regression against the complete pre-existing 133-assertion suite (134th pattern added). **Fase 108** returns to the TCP-connection thread (open since Fase 102, untouched longest of the three open threads) with the smallest safe step toward its own long-flagged "more than one connection" item: a SECOND, sequential call to the exact same, completely unchanged `tcp_echo_test` (Fase 90/102) - zero risk to that already-proven, CI-asserted function, since nothing about it changes here. `tcp_echo_test`'s own `SRC_PORT` (54322) and `INITIAL_SEQ` (0x30000000) are hardcoded internal constants, not parameters, so calling it again reuses the EXACT SAME local port and initial sequence number for a genuinely NEW connection immediately after the first call's own full close - a real, previously-untested scenario against SLIRP's real TCP stack (does it allow immediate same-4-tuple reuse, or does some TIME_WAIT-like state reject a second SYN too soon?). Real local result: the second call reaches the exact same failure point the first call always does locally ("timed out waiting for the real echoed data", the well-known Windows fork() limitation) rather than failing earlier - real partial evidence the port/sequence reuse itself works, even though the full echo can't complete on this machine. Caught and fixed a real, if expected, side effect before shipping: the second real network round trip roughly doubles the wall-clock cost the original 25s->35s CI timeout bump (Fase 90) already accounted for once, cutting off the later ring3_mt-family and final preemptive-scheduler tests by margin on one of five local boots - fixed by bumping the boot-test timeout 35s -> 45s, confirmed clean across all 5 boots afterward. Only the deterministic "second connection attempted" evidence is asserted in CI; whether it actually succeeds (`second_connection_ok`) is logged honestly rather than assumed, the same discipline established for `peer_fin_received`/`answer_count` in prior Fases. Zero regression against the complete pre-existing 134-assertion suite (135th pattern added). **Fase 109** closes a real usability gap rather than continuing any of the 3 open threads: `net::e1000::dns_query_test` (Fase 106/107) and `tcp_echo_test` (Fase 90/102/108) were only ever reachable through the fixed boot self-test sequence, never interactively - unlike ICMP, which has had a `ping` shell command since Fase 49. New `dns A.B.C.D` and `tcpecho A.B.C.D PORT message...` shell commands mirror that exact precedent (`ping`/`netcheck`, Fase 46/49) byte-for-byte: zero changes to either already-proven network function, reused as black boxes. `tcpecho` adds the one genuinely new consideration - a 100-byte user-input length check before calling `tcp_echo_test`, since that function's own doc already flags trusting its caller not to overrun its fixed 256-byte TX slot stride, a real boundary this interactive entry point (unlike the fixed-size boot self-test payloads) must guard itself. Verified with a temporary main.rs call (4 cases: valid DNS query, valid TCP echo, an invalid IP, a missing argument), matching the `ring3syscall` (Fase 72) precedent for verifying an interactive-only command locally before removing the temporary call. `help`'s own command list updated to include both. Zero regression against the complete pre-existing 135-assertion suite - no new CI assertion needed at all, since neither command is auto-invoked at boot (the same reasoning `ring3test`/`ring3syscall` themselves are never CI-asserted either). **Fase 110** closes a real, explicitly-flagged limitation Fase 109's own `dns` shell command doc left open: that command could pick a DIFFERENT target DNS server, but always sent the SAME fixed "A? example.com" query - there was no general-purpose query builder yet. New `net::dns::encode_qname`/`build_query` functions encode an arbitrary caller-supplied hostname into real wire-format DNS labels (length-prefixed segments, RFC 1035 section 3.1) and build a complete query message around it, rejecting an empty hostname, an empty label (a leading/trailing/doubled `.`), or a label over 63 bytes - real caller-input-boundary errors, not something to guess around. `net::e1000::dns_query_test` gained a `hostname: &str` parameter (built via `dns::build_query` instead of a hardcoded 29-byte array), and the `dns` shell command now takes a second `hostname` argument instead of always querying "example.com". A new pure, deterministic self-test (`build_query_selftest`, mirroring Fase 107's own `answer_parse_selftest`) rebuilds Fase 106's original hand-built "example.com" query through the new general-purpose builder and confirms byte-for-byte identity - the real proof this is a genuine generalization, not just a different-shaped function that happens to also produce some query - plus 3 rejection cases. Verified interactively too, beyond the boot self-test: `dns 10.0.2.3 example.org` (a genuinely different hostname than the boot test's own "example.com") round-tripped over the real wire and returned a real, different, plausible IP address (`[172, 66, 157, 237]`) - strong end-to-end evidence the hostname is genuinely encoded onto the wire, not just accepted and ignored. Zero regression against the complete pre-existing 135-assertion suite (136th pattern added), clean on the first attempt (zero clippy/fmt findings, unusual for this arc - most recent Fases needed at least one fix). **Fase 111** returns to the ring-3-mechanism-unification thread (open since Fase 97, untouched for 5 Fases - the longest gap of the three open threads) with a second small, safe slice in the exact same spirit as Fase 97's own `select_next` extraction: `ring3_mt::run_multitasking`'s own inline "arm" block and `ring3::arm_ring3_coop_tasks` (Fase 101) turned out to be the same shape once compared directly - write each task's own (slot value, priority) pair into parallel arrays, then zero the done-mask fresh. New `scheduler::tier_select::arm_task_slots::<N>` (added alongside `select_next` in the same module) is the one real shared function both now call; each mechanism keeps its own ownership story untouched (`ring3_mt` still boxes a fresh 160-byte context per task, `ring3_coop` still takes an already-computed RSP) and its own separate atomic-vs-plain active/current-index state, since only the opaque `u64` slot value and the bookkeeping around it were ever actually identical. Zero changes to either mechanism's context format, asm entry stubs, or per-mechanism static state - exactly the same conservative scope Fase 97 itself committed to. Deliberately behavior-preserving: the original bounds-assert in `run_multitasking` was kept in place (not merely replaced by the shared helper's own redundant one) specifically so panic timing/text stays byte-identical too, not just the happy path. A pure refactor - no new CI assertion, since there is nothing new to observe. Verified directly: all 6 ring-3-scheduling self-tests' own signatures/checksums/done-masks came back byte-identical across all 5 repeated (fresh-disk-image) boots - only `switch_count` varied, the same expected real timing non-determinism this suite has shown since Fase 91/92 - zero regression against the complete pre-existing 136-assertion suite, clean on the first attempt (zero clippy/fmt findings). **Fase 112** returns to the TCP-connection-abstraction thread (open since Fase 102, untouched for 3 Fases) with the structural prerequisite Fase 108's own same-port-reuse test never addressed: `tcp_echo_test`'s `src_port`/`initial_seq` were internal hardcoded constants (`54322`/`0x3000_0000`), meaning two connections could never coexist - a real 4-tuple collision, not just an untested scenario. Generalized both to real parameters (every pre-existing caller - two in `main.rs`, one in `shell.rs`'s `tcpecho` command - updated to keep passing those exact original values, a pure signature widening with zero behavior change for them). A new THIRD sequential boot self-test then puts the newly-exposed parameter to real use: `src_port=54323`/`initial_seq=0x4000_0000`, genuinely distinct from both the default and from `tcp_syn_test`'s own `54321` - a different scenario than Fase 108's same-port reuse, and the real prerequisite before any future attempt at genuinely CONCURRENT connections (still sequential here, not simultaneous, but two different ports must each work correctly before they could ever be tried at the same time). Proactively checked the exact class of timing-margin regression Fase 90 and Fase 108 each hit once before (a new real network round trip that times out locally adds real wall-clock cost that can cut off later tests by margin): confirmed clean across 5 repeated boots at the current 45s CI budget - the final `priority_test` still completes every time, no timeout bump needed this round. New CI assertion for the fully deterministic "third attempt reached" evidence only (137th pattern) - `third_connection_ok` logged, not gated on, the same honest-uncertain-value discipline as Fase 108's own `second_connection_ok`. Zero regression against the complete pre-existing 136-assertion suite, clean on the first attempt (zero clippy/fmt findings). **Fase 113** returns to the DNS-resolver thread (open since Fase 107, untouched for 2 Fases) and closes its own "reusable hostname-lookup API" item: new `net::e1000::resolve_hostname(hostname, dns_server)` wraps `dns_query_test`'s full diagnostic tuple down to the one value an actual caller wants - `Result<[u8; 4], &'static str>` - still not a general resolver (single question, A records only, the same standing scope this whole DNS arc has kept), but a genuinely reusable API rather than a diagnostic-only test function. Integrated it into the `ping` shell command: if the given target isn't a raw dotted-quad, it's now resolved as a hostname via SLIRP's own DNS proxy (10.0.2.3) before pinging - the first time this kernel has ever consumed a DNS resolution for something other than testing DNS itself. Zero risk to either existing boot self-test that calls `ping` (both use literal IPs, so the new hostname-fallback branch is never reached by them). Verified via a temporary main.rs call (matching the `ring3syscall`/Fase 109 precedent): `ping example.com`/`ping bogus.invalid.hostname` both correctly attempt resolution and fail identically on this local machine (the same well-known "zero real DNS answers arrive locally" limitation every DNS Fase since 106 has hit - only the real CI runner's own network path returns actual records); `ping` with no argument still shows usage. Temporary calls removed before commit (confirmed via `git diff main.rs`). No new CI assertion needed - interactive-only, the same reasoning `ring3test`/`dns`/`tcpecho` themselves already established. Zero regression against the complete pre-existing 137-assertion suite across 5 repeated (fresh-disk-image-per-boot) boots, clean on the first attempt (zero clippy/fmt findings). |
| IPv4 + ICMP protocol layer (`net::icmp`) | **Real header/checksum construction and parsing, deliberately not wired to the NIC yet.** `net::icmp` builds and parses real IPv4 headers (RFC 791) and ICMP echo request/reply messages (RFC 792), including the actual RFC 1071 Internet checksum algorithm - big-endian 16-bit word summation, carry-fold, one's-complement, with the odd-byte-padding edge case handled explicitly. Every format detail was checked against the real RFCs rather than relied on from memory, given how a wrong checksum fails silently: the receiving side just never replies, with no crash or error to point at the cause - a harder symptom to root-cause than the e1000 driver's own DD-bit mystery above. Pure byte-buffer logic, no hardware dependency at all - genuinely sending/receiving one over the wire needs a destination MAC address (ARP resolution, which doesn't exist yet) and would touch the e1000 TX/RX paths that are now proven correct and CI-asserted across three Fases; proving these primitives correct in isolation first, before anything depends on them, follows the same order the frame allocator and VFAT entry-building both used. **A real bug surfaced and got fixed during this Fase's own verification, not by chance**: the checksum-verification check initially compared a re-summed, already-checksummed buffer against `0xFFFF` - correct per RFC 1071's own wording, but only when read as "the sum *before* the final one's-complement step". This code's `internet_checksum` function returns that complement directly (`!sum`, the value meant to be *embedded* in a checksum field), so re-running it over a validly-checksummed buffer actually returns `!0xFFFF = 0x0000`, not `0xFFFF` - every genuinely valid buffer was being silently rejected. A pure round-trip test (build a message, then check it with the same function) would never have caught this, since both sides would have agreed with each other while disagreeing with any real peer computing the checksum independently - it was only caught because the self-test also included two independent, hand-computed checksum vectors verified by manual arithmetic before ever running the code. Fixed with a dedicated `checksum_is_valid` helper that encapsulates the correct constant in one place, rather than leaving every call site to compare against the right value itself. Verified: the two hand-computed vectors (one exercising the even-length case, one the odd-byte-padding case) match by hand; a full request/reply built via `build_icmp_echo` (odd-length end to end, so the integration path exercises the same padding logic too) round-trips through `parse_icmp_echo` correctly; a deliberately corrupted reply (one payload byte XOR-flipped) is correctly rejected; and a freshly-built IPv4 header's own checksum verifies too - all six checks passing identically across 3 repeated boots, zero regression across the entire prior self-test suite in the same boot log. |
| TCP protocol layer (`net::tcp`) | **Real header construction/parsing + the RFC 793 pseudo-header checksum (Fase 88), deliberately not wired to any real connection state machine or device I/O yet** - the same "protocol layer first" order `net::icmp` itself followed (Fase 47 before Fase 48-49's real ARP/ping round trips). Reuses `net::icmp::internet_checksum` directly for the actual summing arithmetic (already proven correct there against RFC 1071 - no reason to re-derive it); the only genuinely new logic is assembling TCP's own pseudo-header (source IP + dest IP + a zero byte + protocol number 6 + TCP length) ahead of the real header+payload before handing the combined buffer to that same summing function, per RFC 793 section 3.1. `build_tcp_header`/`parse_tcp_segment` mirror `net::icmp`'s own `build_icmp_echo`/`parse_icmp_echo` shape closely: a 20-byte header (no options, `data_offset=5`), TCP's 6 real flag bits (`FIN`/`SYN`/`RST`/`PSH`/`ACK`/`URG`) as named constants, and a `TcpSegmentInfo` struct returned only after the checksum has already been verified - a caller only ever sees fields it can trust. Applied the EXACT same verification discipline `net::icmp`'s own history already taught this codebase the hard way (a hand-computed checksum vector, not just a round-trip check, since a round trip would still pass even if this code consistently disagreed with the real standard): a synthetic pseudo-header (source `0.0.0.1`, dest `0.0.0.2`, protocol 6, length 4) plus a tiny fake 4-byte "segment" sums to `0x0010` across 8 big-endian words, checksum `= !0x0010 = 0xFFEF` - verified independently (a small standalone calculation, not mental arithmetic alone) before ever writing the assertion that depends on it. Verified: that hand-computed vector matches exactly; a real 20-byte SYN header (`build_tcp_header` with `TCP_FLAG_SYN`, a nonzero sequence number, and a full 16-bit window) round-trips through `parse_tcp_segment` with every field - source/dest port, sequence number, ack number, flags, window - surviving exactly; and a deliberately corrupted header (one byte XOR-flipped after building a valid segment) is correctly rejected by the checksum check rather than silently parsed - all three checks passing identically across 3 repeated boots, zero regression across the complete pre-existing 117-assertion suite. A real TCP handshake (SYN sent, SYN-ACK received and validated, ACK sent, connection considered established) - needing a destination MAC (already solved, `arp_resolve`), a real IPv4 packet (already solved, `net::icmp`'s own `build_ipv4_header`), and genuine connection-state tracking (sequence/acknowledgment number bookkeeping, which does NOT exist yet) - remains real, substantial follow-on work, the same next-step shape ICMP's own echo-request/reply round trip had after Fase 47. **Fase 89 takes the first real step: `net::e1000::tcp_syn_test` sends a genuine SYN and receives back a real, fully-validated SYN-ACK - checksum verified, flags/ports/acknowledgment number all checked - the first real TCP round trip this kernel has ever completed.** Needed a real, deterministic, fully-local destination to handshake with: researched QEMU's own `guestfwd` option (`-netdev user,...,guestfwd=tcp:10.0.2.100:9999-cmd:cat`, verified against the exact pinned QEMU source this kernel already builds against, then verified locally to cause zero regression before becoming a standing part of the boot command, the same two-step discipline Fase 82's own dual-netdev change already established) - QEMU's own SLIRP terminates the connection with its own real, standards-compliant TCP stack and pipes the byte stream to a spawned `cat` process, entirely on the local machine, with zero dependency on real network reachability (the exact same testing philosophy `arp_resolve`/`ping` already established against SLIRP's own fixed gateway). `tcp_syn_test` mirrors `ping`'s own structure line for line (own RX/TX ring setup, `arp_resolve` for the destination MAC, the same TX-poll-DD/RX-poll-and-rearm shape) - the same "duplicate the proven ring-setup shape rather than risk touching it" reasoning `arp_resolve`/`ping` themselves already established. **A real bug surfaced and got fixed getting here, found via a real packet capture (the same `-object filter-dump` technique that resolved the VirtIO RX mystery, Fase 82) after the very first attempt honestly timed out rather than silently claiming success**: SLIRP's own real SYN-ACK carries the standard MSS option (`ip total_len=44`, not the bare-header 40 a Fase-88-style options-free segment would have), making its own `data_offset` 6 words (24 bytes) instead of 5 (20) - `parse_tcp_segment` originally rejected anything but exactly 5 outright, reasonable for Fase 88's own pure, hand-built, options-free unit test but wrong for any genuine reply from a real TCP stack. Separately, `tcp_syn_test` itself was feeding the checksum verifier the ENTIRE remaining receive buffer (which includes real, extra bytes: the TCP options AND further Ethernet-level padding up to the 60-byte minimum frame size) instead of trimming to the exact TCP segment length the IPv4 header's own Total Length field declares - since the original sender's own checksum was computed over only its real bytes, feeding in extra ones made the two sums disagree even though the segment itself was perfectly valid. **Fixed both**: `parse_tcp_segment` now accepts any `data_offset >= 5` (rejecting only an impossible, smaller-than-minimum value; options are skipped, not parsed, since the 6 fields this kernel reads all live in the first 20 bytes regardless), and `tcp_syn_test` computes the real TCP length from the IP header before slicing. Verified after the fix: `syn_ack_received=true ack_matches=true` on the very first attempt post-fix, identical across 3 repeated boots, zero regression against the complete pre-existing 119-assertion suite. Still does not send the final ACK or track any ongoing connection state - proving the send/receive/validate primitives correct in isolation first, the same order e1000 TX (Fase 22/53) preceded RX (Fase 45) by a full Fase gap. **Fase 90 completes the handshake this row's own previous paragraph flagged as still missing: `net::e1000::tcp_echo_test` sends the final ACK, then a real `PSH|ACK` data segment, then verifies a genuine echoed reply carrying the exact same bytes back** - not just a completed handshake, proof the resulting connection is actually usable for real application data. Reuses `tcp_syn_test`'s own SYN/SYN-ACK logic up front (same constants, same independent EtherType/protocol/checksum/source-IP/Total-Length-trim checks), then sends the final ACK (`seq = INITIAL_SEQ+1`, `ack = peer's own ISN+1` - the one piece of state a genuine handshake cannot skip, since it depends on a value only the peer chose) and the data segment through two more TX descriptors at fixed offsets within one shared buffer page, advancing to a fresh descriptor index for each send rather than reusing one (once a descriptor's DD bit is set the ring's tail has already moved past it - advancing is the standard, unambiguous way to queue several sends in sequence, well within this ring's 8-descriptor capacity for just 3 sends). Polls the same single RX descriptor for the echoed reply, filtering specifically for a segment whose ack-number matches what only a genuine receipt of the exact-length data could produce AND whose payload is non-empty - deliberately distinguishing the real echo from any interim bare ACK the peer's stack might send first to acknowledge the data before the echoing process has actually written it back. **Local verification hit a real, environment-specific wall, investigated honestly rather than glossed over**: on this repo's own Windows dev machine, the boot log showed the handshake completing and the final ACK/data segment both going out, but the echo never arrived within the timeout. A real packet capture (the same `-object filter-dump` technique that resolved both the VirtIO RX mystery and Fase 89's own MSS/padding bug) showed exactly why: QEMU itself logs `Slirp: fork_exec: Failed to execute helper program (No such file or directory)` - the `guestfwd=...-cmd:cat` helper process never actually starts on this Windows QEMU build (a POSIX-`fork()`-shaped feature with no real Windows equivalent), so SLIRP tears the connection down itself (a real `FIN` the instant the final ACK arrives, then a real `RST` once the data segment shows up on a connection it's already closing) - not because this kernel sent anything wrong. The capture confirms the opposite, in fact: this kernel's own SYN, final ACK, and data segment all appear on the wire with exactly the right sequence/acknowledgment numbers throughout. This CI runner is `ubuntu-latest`, where `cat` and POSIX `fork()`/`exec()` are native, so the full round trip - including the real echo - is expected to actually complete there for the first time, even though it structurally cannot on this local machine. Locally verified everything that COULD be verified without a working helper process: the complete pre-existing self-test suite unaffected across 3 repeated boots (the only differences being this one new, environment-blocked assertion, plus the already-understood local-disk-reuse artifact repeated non-fresh boots always produce), and byte-exact frame correctness confirmed independently via the packet capture rather than assumed. **The first CI run genuinely failed too - `tcp_echo_test` timed out on `ubuntu-latest` as well, not just locally, honestly reported rather than assumed away.** Rather than guess, a real packet capture (`-object filter-dump`) plus an `actions/upload-artifact` step were added to `kernel-ci.yml` specifically to get ground truth from that exact runner; the resulting capture showed something genuinely revealing - the real echo (`cat`'s own reply, byte-identical, correctly ACKing the exact number of bytes sent) DID arrive on the wire, correctly, about 47ms after the data segment - this kernel's own code simply never saw it. Cross-checked against QEMU's own real `hw/net/e1000.c` (`e1000_has_rxbufs`/`e1000_receive_iov`, fetched at this project's pinned commit): the device only accepts a new frame while `RDH != RDT`; receiving one frame advances `RDH` past the slot it used, and this function's own "rearm" - rewriting that SAME descriptor slot while writing `RDT` with the value it already held - changes nothing from the device's point of view, so the ring silently stays "full" and every later frame is dropped via `e1000_receiver_overrun`, forever. `tcp_syn_test`/`arp_resolve`/`ping` never noticed this same latent pattern because none of them ever need a SECOND real receive in one call; `tcp_echo_test` is the first function in this codebase that does (the stack's own immediate bare ACK, then the real echo). **Fixed** by pre-arming several real, distinct-buffer RX descriptor slots up front (mirroring the TX side's own fixed-offset-per-send approach) instead of ever reusing one slot mid-flight - slot 0 for the SYN-ACK, slots 1+ polled in arrival order for the echo, each slot's one real buffer read exactly once. Verified after the fix: the same packet-capture-confirmed frame correctness held, the complete pre-existing self-test suite remained unaffected across 2 more repeated local boots, and the packet-capture infrastructure added to investigate this was kept as a standing part of CI rather than reverted - a debugging tool this exact TCP work has now needed three separate times. **The next CI run confirmed the fix itself is genuinely correct**: `tcp_echo_test ... echo_matches=true` appeared in the real boot log (independently reconstructed from the raw job log and re-verified with the exact same grep pattern CI uses, removing any doubt) - the real echo is now actually received, for the first time. That same run still failed, but on a completely different, unrelated assertion (`priority_test`, Fase 87) - genuine progress exposed a genuine margin problem: `tcp_echo_test`'s own real network round trip, now actually succeeding instead of failing fast, takes real wall-clock time, and it lands late in the boot sequence right before `priority_test`'s own ~2.7s real timer wait - together they no longer fit inside the 25-second QEMU timeout this workflow has used since Fase 82. Confirmed locally (35s lets `priority_test` finish completely with room to spare, unlike 25s) before bumping the budget. **CI confirmed fully green afterward** - the complete suite, including the real echo, passes on `ubuntu-latest` for the first time. This closes out the largest investigation this session has needed since the VirtIO RX mystery: an honestly-reported CI failure, a real packet capture revealing the true (and genuinely surprising) root cause, a fix verified against QEMU's own real source rather than guessed, and a second, unrelated margin issue found and fixed the same way rather than papered over. **Fase 102 closes a gap every TCP test before it left open: the connection was never actually closed.** `tcp_echo_test` now sends a real `FIN|ACK` right after the echo (consuming one sequence number, per RFC 793's own convention) and, if the peer's own FIN arrives back within the same generous timeout the rest of the function already uses, completes a real 4-way close with a final ACK - `NUM_RX_SLOTS` bumped from 4 to 7 for the extra headroom this needs beyond the handshake+echo. Genuinely uncertain going in whether SLIRP's `cmd:cat` target replies with a distinguishable FIN once its own stdin sees EOF (never tested before this Fase, and its exact timing - combined with the ACK of our own FIN or separate - wasn't known either), so the new CI assertion only gates on `fin_sent=true` (deterministic: reaching that line at all means it was sent) while `peer_fin_received`'s own specific value is observed and honestly recorded rather than assumed. Keeps the function's existing signature and its already-proven `echo_matches=true` assertion completely untouched - purely additive. Cannot be verified locally at all (the same permanent Windows `guestfwd`/fork() limitation `tcp_syn_test`/`tcp_echo_test` have always had), so CI is the only real verification surface here, same as this row's own Fase 89/90 history. |
| Syscall dispatcher | Still minimal, but one real number now genuinely does something beyond a stub. **`SYS_TENSOR_EVAL` (Fase 55) had been defined since before this session even started, but `dispatch_syscall` never had a match arm for it at all - calling it always fell through to "Unknown System Call Number", a genuine dead constant, the same class of gap Fase 50 found in `GgufTensorInfo`'s own long-parsed-but-unused `gtype` field.** Now wires to a real `TensorEngine::matmul_layer` call: a new `TensorEvalArgs` struct (weights/inputs/bias/outputs pointers+lengths, plus the two dimensions) is passed as a single pointer via `arg1`, since a real forward-pass layer needs more real parameters than 3 scalar syscall args could ever encode - the same reason real syscall ABIs commonly pass a request-struct pointer once a call outgrows a couple of plain numbers. Deliberately scoped as a small, ring-0-only slice of "make syscalls real", distinct from the much larger, still-deferred undertaking of an actual ring-3/ring-0 transition (this kernel has no user-mode GDT segments, no `RSP0` configured, and no `SYSCALL`/`SYSRET` or `DPL=3` interrupt gate at all - confirmed by directly reading `gdt.rs` before deferring that larger piece three Fases running). Verified with a small, fully hand-computable 2×2 layer (`weights=[1,2,3,4]`, `inputs=[1,1]`, `bias=[1,-10]`) that deliberately exercises ReLU's clipping branch too, not just addition: row 0 = `ReLU(1·1+2·1+1)=4.0`, row 1 = `ReLU(3·1+4·1-10)=ReLU(-3)=0.0` - both `syscall_ok` (the dispatcher returned its real success code, not the `u64::MAX` "unknown syscall" fallback it always had before) and `tensor_eval_correct` (the actual computed outputs match these hand-computed values exactly) passed on the very first boot, identically across 3 repeated boots, zero regression across the entire prior self-test suite. **Fase 74 closes a real gap Fase 55's own scope note above didn't need to worry about yet, but the ring-3 arc (Fase 68-73) has since made concrete: `SYS_TENSOR_EVAL`'s pointer argument was dereferenced completely unconditionally - now that ring-3 code can genuinely invoke syscalls with real, caller-controlled arguments (Fase 72) and cleanly return afterward (Fase 73), a bad pointer here (accidental or deliberate) would trigger a real `#PF` from INSIDE the syscall handler, hitting the existing page-fault handler, which halts the kernel forever - turning one bad syscall argument into a dead machine, not a rejected call.** Fixed with a new `memory::paging` module exposing the kernel's page-table mapper globally (the same `Mutex<Option<T>>` + `install_global` pattern `frame_allocator.rs` already established - only one `OffsetPageTable` can safely exist at a time, since it wraps a unique `&'static mut PageTable`, so this *replaces* `kernel_main`'s own local variable entirely rather than existing alongside it) and a new `pointer_is_mapped` check, run against `TensorEvalArgs` itself and each of its four embedded slice pointers before any of them are dereferenced. Deliberately uses `VirtAddr::try_new`, not `VirtAddr::new` - the latter panics on a non-canonical address, which an arbitrary `u64` from a syscall caller can easily be, and a validation function that itself panics on bad input defeats its own purpose. Deliberately checks only `PRESENT`, not `USER_ACCESSIBLE`: `dispatch_syscall` is reachable from both ring-0 (this kernel's own self-tests, using ordinary non-user-accessible heap memory) and ring-3, and doesn't yet track which one a given call came from - enforcing "ring-3 may only pass pointers to memory it could otherwise access" needs that tracked first, real, separate follow-on work. Verified both directions: the existing valid-pointer test above still passes unchanged, and a new test calling `SYS_TENSOR_EVAL` with a deliberately unmapped address (`0x1000` - low, canonical, and far below every real mapping this kernel has: the kernel image, the heap, Fase 70's user page) gets a clean `u64::MAX` back, with the kernel's own execution continuing normally to every self-test after it - which is itself part of the proof, since the failure this Fase prevents would have looked exactly like a hang, not a returned error. Byte-for-byte identical across 3 repeated boots. **Fase 76 closes the gap Fase 74 explicitly deferred, now that Fase 75 made it possible: `SYS_TENSOR_EVAL` requires `USER_ACCESSIBLE`, not just `PRESENT`, on every pointer it dereferences - but ONLY when the real, hardware-reported caller privilege (Fase 75's own `caller_rpl`) says the call genuinely came from ring-3.** `dispatch_syscall` gained a new `caller_is_ring3: bool` parameter, threaded from `handle_real_syscall` (`caller_rpl == 3`) down to a new `pointer_is_user_accessible` check in `memory::paging` (same shape as Fase 74's own `pointer_is_mapped`, this one also requiring the `USER_ACCESSIBLE` flag). This closes a real "confused deputy" gap Fase 74's own weaker check couldn't: real, `PRESENT` kernel-only memory (the heap, in particular) legitimately passes `pointer_is_mapped`, but a ring-3 caller pointing at it should NOT be trusted the way this kernel's own ring-0 self-tests legitimately are (those still use ordinary heap/stack memory, and are unaffected since `caller_is_ring3` is false for them). Verified with a genuine ring-3 program - `mov rax,4` (`SYS_TENSOR_EVAL`) / `mov rdi,<addr>` (both needing real 64-bit immediates, since this kernel's addresses exceed 32 bits) / `int 0x80` / `int 0x81`, reusing Fase 73's own safe-exit mechanism to carry the syscall's own return value back untouched - pointing a real `TensorEvalArgs` at `memory::heap::HEAP_START` and getting a clean `u64::MAX` back, while the SAME syscall's existing ring-0 test (above) keeps passing unchanged. Byte-for-byte identical across 3 repeated boots. **Fase 77 closes the last gap explicitly named after Fase 76: every pointer check above only ever looked at a slice's STARTING address - a slice that begins on a genuinely valid, correctly-privileged page can still run off the end into an unmapped (or, for ring-3, non-user-accessible) one if its stated length is wrong or hostile.** `memory::paging` gained `range_is_mapped`/`range_is_user_accessible` (replacing the single-address `pointer_is_mapped`/`pointer_is_user_accessible` entirely, confirmed via grep they had no other callers), walking EVERY page a `[ptr, ptr + len_bytes)` range touches via the same underlying flag-check primitive - one address per page is enough, since `PRESENT`/`USER_ACCESSIBLE` apply uniformly across a whole page, never partway through one. `syscall.rs` now computes REAL byte lengths (`len * size_of::<f32>()`, via `saturating_mul` so a hostile `len` can't overflow the multiplication and wrap to a small, wrongly-passing value) for each of `SYS_TENSOR_EVAL`'s four slice pointers, and the args struct's own check now uses its real `size_of::<TensorEvalArgs>()` instead of a placeholder `1`. Verified with a real ring-3 program whose `weights` pointer sits only 4 bytes before the user page's own end (`USER_TEST_PAGE_ADDR + 4092` - itself genuinely `PRESENT` and `USER_ACCESSIBLE`, so the OLD starting-address-only check would have WRONGLY ACCEPTED it) but whose `weights_len = 2` asks for 8 bytes - the extra 4 spill into the next page, which Fase 70's own single, deliberately isolated 4 KiB mapping never extended into. Gets the same clean `u64::MAX` back as every other rejection case, while all three earlier `SYS_TENSOR_EVAL` tests (valid ring-0, unmapped, kernel-only-from-ring-3) keep passing completely unchanged. Byte-for-byte identical across 3 repeated boots. This completes the Fase 74-75-76-77 "make syscalls safe against a real ring-3 caller" mini-arc: unmapped pointers rejected, caller privilege known for real, kernel-only pointers rejected for ring-3 callers, and now a slice's whole length actually checked, not just where it starts. |

Nothing above is faked to look more finished than it is — this table is meant to stay accurate as the project grows, not to be a wishlist.

## Building & running the kernel

Requires the **nightly** Rust toolchain (pinned per-directory via `rust-toolchain.toml`) and [QEMU](https://www.qemu.org/).

```bash
# 1. Build the kernel itself
cd agentos-kernel
cargo build --release

# 2. Package it into a bootable disk image (separate crate, host target)
cd ../kernel-runner
cargo build --release
./target/release/kernel-runner "../agentos-kernel/target/x86_64-unknown-none/release/agentos-kernel" "../agentos-kernel/target/disk-image"

# 3. Boot it
qemu-system-x86_64 -drive format=raw,file=../agentos-kernel/target/disk-image/agentos-bios.img
```

Or just run [`agentos-kernel/boot_kernel.bat`](agentos-kernel/boot_kernel.bat), which automates all three steps and opens an interactive QEMU window.

For a headless run with serial output captured to your terminal instead:

```bash
qemu-system-x86_64 -drive format=raw,file=agentos-kernel/target/disk-image/agentos-bios.img -serial stdio -display none -no-reboot -no-shutdown
```

## Running the TypeScript dashboard simulator

```bash
npm install
npm run build
npm start
```

Serves the mocked dashboard UI in `public/` against the in-memory "kernel" in `src/server/kernel.ts`. Nothing here talks to real hardware or a real model.

## Repository layout

```
agentos-kernel/   Rust #![no_std] kernel - GDT/IDT, paging/heap, scheduler, syscalls, drivers
kernel-runner/    Host-target helper crate that packages the kernel ELF into a bootable disk image
src/, public/     TypeScript/Express dashboard simulator (parked, not integrated with the kernel)
```

## License

MIT - see [LICENSE](LICENSE).
