[package]
name = "kernel-runner"
version = "0.1.0"
edition = "2021"
description = "Host-side tool: packages the agentos-kernel ELF into bootable BIOS/UEFI disk images via the `bootloader` crate."

[dependencies]
bootloader = "0.11.17"
